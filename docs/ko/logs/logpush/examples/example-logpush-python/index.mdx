---
pcx_content_type: how-to
title: Python로 Logpush 관리
sidebar:
  order: 89
translation_locale: ko-KR
translation_source_html: >-
  https://developers.cloudflare.com/logs/logpush/examples/example-logpush-python/
translation_source_md: >-
  https://developers.cloudflare.com/logs/logpush/examples/example-logpush-python/index.md
translation_source_kind: repo-mdx
translation_snapshot: '2026-03-13'
translation_upstream_ref: 66ee2dcd3715f1ca46e70954a3cda63f6cbebc75

---
Python를 사용하여 Cloudflare Logpush 서비스를 관리 할 수 있습니다. 아래 스크립트에서 작업, 검색 작업 세부 사항, 업데이트 작업 설정, 그리고 Logpush 작업을 삭제할 수 있는 예를 찾을 수 있습니다.

:::참조

아래 예제는 Zone-scoped datasets입니다. account-scoped datasets는 사용해야 합니다`<ACCOUNT_ID>`대신에`<ZONE_ID>`.

:::

```python
import json
import requests

url = "https://api.cloudflare.com/client/v4/"

x_auth_email = "<EMAIL>"
x_auth_key = "<API_KEY>"

zone_id = "<ZONE_ID>"
destination_conf = "s3://<BUCKET_NAME>/logs?region=us-west-1"

logpush_url = url + "/zones/%s/logpush" % zone_id

headers = {
  'X-Auth-Email': <EMAIL>,
  'X-Auth-Key': <API_KEY>,
  'Content-Type': 'application/json'
}

#작업 만들기
r = requests.post(logpush_url + "/jobs", headers=headers, data=json.dumps({"destination_conf":destination_conf}))
print(r.status_code, r.text)
assert r.status_code == 201
assert r.json()["result"]["enabled"] == False

#새로운 직업의 ID 유지
id = r.json()["result"]["id"]

#작업하기
r = requests.get(logpush_url + "/jobs/%s" % id, headers=headers)
print(r.status_code, r.text)
assert r.status_code == 200

#지역을 위한 모든 일을 얻으십시오
r = requests.get(logpush_url + "/jobs", headers=headers)
print(r.status_code, r.text)
assert r.status_code == 200
assert len(r.json()["result"]) > 0

#업데이트 작업
r = requests.put(logpush_url + "/jobs/%s" % id, headers=headers, data=json.dumps({"enabled":True}))
print(r.status_code, r.text)
assert r.status_code == 200
assert r.json()["result"]["enabled"] == True

#작업 삭제
r = requests.delete(logpush_url + "/jobs/%s" % id, headers=headers)
print(r.status_code, r.text)
assert r.status_code == 200
```
