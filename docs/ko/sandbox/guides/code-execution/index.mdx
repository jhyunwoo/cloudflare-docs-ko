---
title: 코드 해석기
pcx_content_type: how-to
sidebar:
  order: 5
description: 풍부한 출력을 가진 Python 및 JavaScriptXQ 코드를 실행하십시오.
translation_locale: ko-KR
translation_source_html: 'https://developers.cloudflare.com/sandbox/guides/code-execution/'
translation_source_md: 'https://developers.cloudflare.com/sandbox/guides/code-execution/index.md'
translation_source_kind: repo-mdx
translation_snapshot: '2026-03-13'
translation_upstream_ref: 66ee2dcd3715f1ca46e70954a3cda63f6cbebc75

---
import { TypeScriptExample } from "~/components";

이 가이드는 Python 및 JavaScript 코드를 코드 Interpreter API를 사용하여 풍부한 출력으로 실행하는 방법을 보여줍니다.

## 코드 해석기를 사용할 때

Code Interpreter API 사용**간단한, 직접 코드 실행**최소 설정으로:

- **빠른 코드 실행**- 환경 설정 없이 Python/JS 코드를 실행
- **부유한 산출**- 차트, 표, 이미지, HTML을 자동으로 가져옵니다
- **AI 생성 코드**- 구조화된 결과를 가진 LLM-generated 부호를 실행하십시오
- **Persistent 상태**- 동일한 컨텍스트에서 실행 사이에 보존 된 변수

제품 정보`exec()`제품 정보**고급 또는 사용자 정의 워크플로우**:

- **시스템 운영**- 패키지 설치, 파일 관리, 빌드 실행
- **주문 환경**- 특정 버전 구성, 의존
- **Shell 명령**- Git 가동, 체계 유틸리티, 복잡한 파이프라인
- **긴 실행 과정**- 배경 서비스, 서버

## 실행 상황 만들기

Code contexts는 실행 중 상태를 유지:

<TypeScriptExample>
  ```
  import { getSandbox } from '@cloudflare/sandbox';

  const sandbox = getSandbox(env.Sandbox, 'my-sandbox');

  // Create a Python context
  const pythonContext = await sandbox.createCodeContext({
    language: 'python'
  });

  console.log('Context ID:', pythonContext.id);
  console.log('Language:', pythonContext.language);

  // Create a JavaScript context
  const jsContext = await sandbox.createCodeContext({
    language: 'javascript'
  });
  ```
</TypeScriptExample>

## 실행 코드

### 간단한 실행

<TypeScriptExample>
  ```
  // Create context
  const context = await sandbox.createCodeContext({
    language: 'python'
  });

  // Execute code
  const result = await sandbox.runCode(`
  print("Hello from Code Interpreter!")
  result = 2 + 2
  print(f"2 + 2 = {result}")
  `, { context: context.id });

  console.log('Output:', result.output);
  console.log('Success:', result.success);
  ```
</TypeScriptExample>

### 상태 내에서 context

변수 및 수입은 동일한 컨텍스트에서 실행 사이에 사용할 수 있습니다. 컨테이너가 활성 상태로 유지됩니다.

<TypeScriptExample>
  ```
  const context = await sandbox.createCodeContext({
    language: 'python'
  });

  // First execution - import and define variables
  await sandbox.runCode(`
  import pandas as pd
  import numpy as np

  data = [1, 2, 3, 4, 5]
  print("Data initialized")
  `, { context: context.id });

  // Second execution - use previously defined variables
  const result = await sandbox.runCode(`
  mean = np.mean(data)
  print(f"Mean: {mean}")
  `, { context: context.id });

  console.log(result.output); // "Mean: 3.0"
  ```
</TypeScriptExample>

:::참조
Context 상태는 inactivity 때문에 컨테이너가 재시작하면 손실됩니다. 중요한 자료의 경우, sandbox 외부의 결과를 저장하거나 필요에 reinitialize 코드를 디자인하십시오.
주요 특징

## 손잡이 부유한 산출

코드 해석기는 여러 출력 형식을 반환:

<TypeScriptExample>
  ```
  const result = await sandbox.runCode(`
  import matplotlib.pyplot as plt

  plt.plot([1, 2, 3], [1, 4, 9])
  plt.title('Simple Chart')
  plt.show()
  `, { context: context.id });

  // Check available formats
  console.log('Formats:', result.formats);  // ['text', 'png']

  // Access outputs
  if (result.outputs.png) {
    // Return as image
    return new Response(atob(result.outputs.png), {
      headers: { 'Content-Type': 'image/png' }
    });
  }

  if (result.outputs.html) {
    // Return as HTML (pandas DataFrames)
    return new Response(result.outputs.html, {
      headers: { 'Content-Type': 'text/html' }
    });
  }

  if (result.outputs.json) {
    // Return as JSON
    return Response.json(result.outputs.json);
  }
  ```
</TypeScriptExample>

## 스트림 실행 출력

긴 실행 코드를 위해, 실시간 스트림 출력:

<TypeScriptExample>
  ```
  const context = await sandbox.createCodeContext({
    language: 'python'
  });

  const result = await sandbox.runCode(
    `
  import time

  for i in range(10):
      print(f"Processing item {i+1}/10...")
      time.sleep(0.5)

  print("Done!")
  `,
    {
      context: context.id,
      stream: true,
      onOutput: (data) => {
        console.log('Output:', data);
      },
      onResult: (result) => {
        console.log('Result:', result);
      },
      onError: (error) => {
        console.error('Error:', error);
      }
    }
  );
  ```
</TypeScriptExample>

## AI-generated 코드 실행

Sandbox에서 LLM 생성 된 코드를 안전하게 실행하십시오.

<TypeScriptExample>
  ```
  // 1. Generate code with Claude
  const response = await fetch('https://api.anthropic.com/v1/messages', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'x-api-key': env.ANTHROPIC_API_KEY,
      'anthropic-version': '2023-06-01'
    },
    body: JSON.stringify({
      model: 'claude-3-5-sonnet-20241022',
      max_tokens: 1024,
      messages: [{
        role: 'user',
        content: 'Write Python code to calculate fibonacci sequence up to 100'
      }]
    })
  });

  const { content } = await response.json();
  const code = content[0].text;

  // 2. Execute in sandbox
  const context = await sandbox.createCodeContext({ language: 'python' });
  const result = await sandbox.runCode(code, { context: context.id });

  console.log('Generated code:', code);
  console.log('Output:', result.output);
  console.log('Success:', result.success);
  ```
</TypeScriptExample>

## 자주 묻는 질문

### 모든 컨텍스트 목록

<TypeScriptExample>
  ```
  const contexts = await sandbox.listCodeContexts();

  console.log(`${contexts.length} active contexts:`);

  for (const ctx of contexts) {
    console.log(`  ${ctx.id} (${ctx.language})`);
  }
  ```
</TypeScriptExample>

### 연락처

<TypeScriptExample>
  ```
  // Delete specific context
  await sandbox.deleteCodeContext(context.id);
  console.log('Context deleted');

  // Clean up all contexts
  const contexts = await sandbox.listCodeContexts();
  for (const ctx of contexts) {
    await sandbox.deleteCodeContext(ctx.id);
  }
  console.log('All contexts deleted');
  ```
</TypeScriptExample>

## 좋은 관행

- **연락처**- 무료 리소스로 수행 할 때 context 삭제
- **핸들 오류**- 항상 체크`result.success`·`result.error`
- **Stream 긴 가동**- 코드의 스트리밍 사용 >2 초
- **AI 코드 검증**- 실행하기 전에 생성 된 코드를 검토

## 관련 자료

- [부호 해석 API reference](/sandbox/api/interpreter/)- 완전한 API 문서
- [AI 코드 executor 튜토리얼](/sandbox/tutorials/ai-code-executor/)- 완전한 AI executor 구축
- [명령 가이드 실행](/sandbox/guides/execute-commands/)- 저수준 명령 실행
