---
summary: >-
  Return JSON directly from a Worker script, useful for building APIs and
  middleware.
demo: 'https://returning-json.workers-sites-examples.workers.dev'
tags:
  - JSON
  - JavaScript
  - TypeScript
  - Python
  - Rust
preview:
  - true
pcx_content_type: example
title: JSON을 반환
reviewed: 2024-01-11T00:00:00.000Z
sidebar:
  order: 2
description: Worker 스크립트에서 JSON을 직접 반환하고 API와 Middleware를 구축하는 데 유용합니다.
translation_locale: ko-KR
translation_source_html: 'https://developers.cloudflare.com/workers/examples/return-json/'
translation_source_md: 'https://developers.cloudflare.com/workers/examples/return-json/index.md'
translation_source_kind: repo-mdx
translation_snapshot: '2026-03-13'
translation_upstream_ref: 66ee2dcd3715f1ca46e70954a3cda63f6cbebc75

---
get started를 신속히 원한다면 아래 버튼을 클릭하십시오.

[![Cloudflare에 배포](https://deploy.workers.cloudflare.com/button)](https://deploy.workers.cloudflare.com/?url=https://github.com/cloudflare/docs-examples/tree/main/workers/return-json)

GitHub 계정에서 저장소를 생성하고 Cloudflare Workers에 응용 프로그램을 배포합니다.

import { Render, TabItem, Tabs } from "~/components";

<Tabs syncKey="workersExamples">
  <TabItem label="JavaScript" icon="seti:javascript">
    <Render file="return-json-example-js" product="workers" />
  </TabItem>

  <TabItem label="TypeScript" icon="seti:typescript">
    ```ts
    export default {
    	async fetch(request): Promise<Response> {
    		const data = {
    			hello: "world",
    		};

    		return Response.json(data);
    	},
    } satisfies ExportedHandler;
    ```
  </TabItem>

  <TabItem label="Python" icon="seti:python">
    ```py
    from workers import WorkerEntrypoint, Response
    import json

    class Default(WorkerEntrypoint):
        def fetch(self, request):
            data = json.dumps({"hello": "world"})
            headers = {"content-type": "application/json"}
            return Response(data, headers=headers)
    ```
  </TabItem>

  <TabItem label="Rust" icon="seti:rust">
    ```rs
    use serde::{Deserialize, Serialize};
    use worker::*;

    #[derive(Deserialize, Serialize, Debug)]
    struct Json {
        hello: String,
    }

    #[event(fetch)]
    async fn fetch(_req: Request, _env: Env, _ctx: Context) -> Result<Response> {
        let data = Json {
            hello: String::from("world"),
        };
        Response::from_json(&data)
    }
    ```
  </TabItem>

  <TabItem label="호노" icon="seti:typescript">
    ```ts
    import { Hono } from "hono";

    const app = new Hono();

    app.get("*", (c) => {
    	const data = {
    		hello: "world",
    	};

    	return c.json(data);
    });

    export default app;
    ```
  </TabItem>
</Tabs>
