---
summary: >-
  Example of how to add, change, or delete headers sent in a request or returned
  in a response.
tags:
  - Headers
  - Middleware
  - JavaScript
  - TypeScript
  - Python
preview:
  - true
pcx_content_type: example
title: Alter 헤더
sidebar:
  order: 1001
description: '추가하는 방법의 예, 변경, 또는 헤더를 삭제 요청 또는 응답에 반환.'
reviewed: 2020-09-30T00:00:00.000Z
translation_locale: ko-KR
translation_source_html: 'https://developers.cloudflare.com/workers/examples/alter-headers/'
translation_source_md: 'https://developers.cloudflare.com/workers/examples/alter-headers/index.md'
translation_source_kind: repo-mdx
translation_snapshot: '2026-03-13'
translation_upstream_ref: 66ee2dcd3715f1ca46e70954a3cda63f6cbebc75

---
get started를 빨리 원하면 아래 버튼을 클릭하십시오.

[![Cloudflare에 배포](https://deploy.workers.cloudflare.com/button)](https://deploy.workers.cloudflare.com/?url=https://github.com/cloudflare/docs-examples/tree/main/workers/alter-headers)

GitHub 계정에서 저장소를 생성하고 Cloudflare Workers에 응용 프로그램을 배포합니다.

import { TabItem, Tabs } from "~/components";

<Tabs syncKey="workersExamples">
  <TabItem label="JavaScript" icon="seti:javascript">
    ```js
    export default {
    	async fetch(request) {
    		const response = await fetch("https://example.com");

    		//더 이상 immutable 인 응답을 복제
    		const newResponse = new Response(response.body, response);

    		//사용자 정의 헤더를 값으로 추가
    		newResponse.headers.append(
    			"x-workers-hello",
    			"Hello from Cloudflare Workers",
    		);

    		//맨 위
    		newResponse.headers.delete("x-header-to-delete");
    		newResponse.headers.delete("x-header2-to-delete");

    		//기존 헤더의 값을 조정
    		newResponse.headers.set("x-header-to-change", "NewValue");

    		return newResponse;
    	},
    };
    ```
  </TabItem>

  <TabItem label="TypeScript" icon="seti:typescript">
    ```ts
    export default {
    	async fetch(request): Promise<Response> {
    		const response = await fetch(request);

    		//더 이상 immutable 인 응답을 복제
    		const newResponse = new Response(response.body, response);

    		//사용자 정의 헤더를 값으로 추가
    		newResponse.headers.append(
    			"x-workers-hello",
    			"Hello from Cloudflare Workers",
    		);

    		//맨 위
    		newResponse.headers.delete("x-header-to-delete");
    		newResponse.headers.delete("x-header2-to-delete");

    		//기존 헤더의 값을 조정
    		newResponse.headers.set("x-header-to-change", "NewValue");

    		return newResponse;
    	},
    } satisfies ExportedHandler;
    ```
  </TabItem>

  <TabItem label="Python" icon="seti:python">
    ```py
    from workers import Response, fetch, WorkerEntrypoint

    class Default(WorkerEntrypoint):
      async def fetch(self, request):
          response = await fetch("https://example.com")

          #응답 헤더를 잡아 그래서 그들은 수정 될 수있다
          new_headers = response.headers

          #사용자 정의 헤더를 값으로 추가
          new_headers["x-workers-hello"] = "Hello from Cloudflare Workers"

          #맨 위
          if "x-header-to-delete" in new_headers:
              del new_headers["x-header-to-delete"]
          if "x-header2-to-delete" in new_headers:
              del new_headers["x-header2-to-delete"]

          #기존 헤더의 값을 조정
          new_headers["x-header-to-change"] = "NewValue"

          return Response(response.body, headers=new_headers)
    ```
  </TabItem>

  <TabItem label="호노" icon="seti:typescript">
    ```ts
    import { Hono } from 'hono';

    const app = new Hono();

    app.use('*', async (c, next) => {
      //다음 미들웨어 / 핸들러와 요청을 처리
      await next();

      //응답이 생성된 후, 헤더를 수정할 수 있습니다.

      //사용자 정의 헤더를 값으로 추가
      c.res.headers.append(
        "x-workers-hello",
        "Hello from Cloudflare Workers with Hono"
      );

      //맨 위
      c.res.headers.delete("x-header-to-delete");
      c.res.headers.delete("x-header2-to-delete");

      //기존 헤더의 값을 조정
      c.res.headers.set("x-header-to-change", "NewValue");
    });

    app.get('*', async (c) => {
      //example.com에서 Fetch 내용
      const response = await fetch("https://example.com");

      //본래 우두머리를 가진 응답 몸을 돌려보냅니다
      //(우리의 미들웨어는 보내기 전에 헤더를 수정합니다)
      return new Response(response.body, {
        headers: response.headers
      });
    });

    export default app;
    ```
  </TabItem>
</Tabs>

또한 사용할 수 있습니다[`custom-headers-example`관련 기사](https://github.com/kristianfreeman/custom-headers-example)이 코드를 사용자 정의 도메인에 배포합니다.
