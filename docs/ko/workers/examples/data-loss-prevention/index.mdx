---
summary: >-
  Protect sensitive data to prevent data loss, and send alerts to a webhooks
  server in the event of a data breach.
tags:
  - Security
  - JavaScript
  - TypeScript
  - Python
pcx_content_type: example
title: 데이터 손실 방지
sidebar:
  order: 1001
description: 데이터 손실을 방지하기 위해 민감한 데이터를 보호하고 데이터 침해 사건에 webhooks 서버에 경고를 보냅니다.
reviewed: 2020-09-30T00:00:00.000Z
translation_locale: ko-KR
translation_source_html: 'https://developers.cloudflare.com/workers/examples/data-loss-prevention/'
translation_source_md: >-
  https://developers.cloudflare.com/workers/examples/data-loss-prevention/index.md
translation_source_kind: repo-mdx
translation_snapshot: '2026-03-13'
translation_upstream_ref: 66ee2dcd3715f1ca46e70954a3cda63f6cbebc75

---
get started를 빨리 원하면 아래 버튼을 클릭하십시오.

[![Cloudflare에 배포](https://deploy.workers.cloudflare.com/button)](https://deploy.workers.cloudflare.com/?url=https://github.com/cloudflare/docs-examples/tree/main/workers/data-loss-prevention)

GitHub 계정에서 저장소를 생성하고 Cloudflare WorkersXQ에 응용 프로그램을 배포합니다.

import { TabItem, Tabs } from "~/components";

<Tabs syncKey="workersExamples">
  <TabItem label="JavaScript" icon="seti:javascript">
    ```js
    export default {
    	async fetch(request) {
    		const DEBUG = true;
    		const SOME_HOOK_SERVER = "https://webhook.flow-wolf.io/hook";

    		/** 이름
    * 웹훅 서버에 게시하여 데이터 침해를 경고*/
    		async function postDataBreach(request) {
    			return await fetch(SOME_HOOK_SERVER, {
    				method: "POST",
    				headers: {
    					"content-type": "application/json;charset=UTF-8",
    				},
    				body: JSON.stringify({
    					ip: request.headers.get("cf-connecting-ip"),
    					time: Date.now(),
    					request: request,
    				}),
    			});
    		}

    		/** 이름
    * 일반 표현으로 개인 데이터를 정의합니다.
    * 신용 카드 데이터 및 스트립 경우 블록과 응답
    * 응답에서 이메일 및 전화 번호.
    * 실행은 MIME 유형 "text/*"로 제한됩니다.*/
    		const response = await fetch(request);

    		//응답이 텍스트가없는 경우 반환 기원 응답
    		const contentType = response.headers.get("content-type") || "";
    		if (!contentType.toLowerCase().includes("text/")) {
    			return response;
    		}

    		let text = await response.text();

    		//디버깅이 응답을 대체 할 때
    		//이메일:
    		text = DEBUG
    			? text.replace("You may use this", "me@example.com may use this")
    			: text;
    		const sensitiveRegexsMap = {
    			creditCard: String.raw`\b(?:4[0-9]{12}(?:[0-9]{3})?|(?:5[1-5][0-9]{2}|222[1-9]|22[3-9][0-9]|2[3-6][0-9]{2}|27[01][0-9]|2720)[0-9]{12}|3[47][0-9]{13}|3(?:0[0-5]|[68][0-9])[0-9]{11}|6(?:011|5[0-9]{2})[0-9]{12}|(?:2131|1800|35\d{3})\d{11})\b`,
    			email: String.raw`\b[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}\b`,
    			phone: String.raw`\b07\d{9}\b`,
    		};

    		for (const kind in sensitiveRegexsMap) {
    			const sensitiveRegex = new RegExp(sensitiveRegexsMap[kind], "ig");
    			const match = await sensitiveRegex.test(text);
    			if (match) {
    				//데이터 침해 경고
    				await postDataBreach(request);
    				//신용 카드 경우 블록과 응답,
    				//그렇지 않으면 `*`S와 민감한 텍스트를 교체
    				return kind === "creditCard"
    					? new Response(kind + " found\nForbidden\n", {
    							status: 403,
    							statusText: "Forbidden",
    						})
    					: new Response(text.replace(sensitiveRegex, "**********"), response);
    			}
    		}
    		return new Response(text, response);
    	},
    };
    ```
  </TabItem>

  <TabItem label="TypeScript" icon="seti:typescript">
    ```ts
    export default {
    	async fetch(request): Promise<Response> {
    		const DEBUG = true;
    		const SOME_HOOK_SERVER = "https://webhook.flow-wolf.io/hook";

    		/** 이름
    * 웹훅 서버에 게시하여 데이터 침해를 경고*/
    		async function postDataBreach(request) {
    			return await fetch(SOME_HOOK_SERVER, {
    				method: "POST",
    				headers: {
    					"content-type": "application/json;charset=UTF-8",
    				},
    				body: JSON.stringify({
    					ip: request.headers.get("cf-connecting-ip"),
    					time: Date.now(),
    					request: request,
    				}),
    			});
    		}

    		/** 이름
    * 일반 표현으로 개인 데이터를 정의합니다.
    * 신용 카드 데이터 및 스트립 경우 블록과 응답
    * 응답에서 이메일 및 전화 번호.
    * 실행은 MIME 유형 "text/*"로 제한됩니다.*/
    		const response = await fetch(request);

    		//응답이 텍스트가없는 경우 반환 기원 응답
    		const contentType = response.headers.get("content-type") || "";
    		if (!contentType.toLowerCase().includes("text/")) {
    			return response;
    		}

    		let text = await response.text();

    		//디버깅이 응답을 대체 할 때
    		//이메일:
    		text = DEBUG
    			? text.replace("You may use this", "me@example.com may use this")
    			: text;
    		const sensitiveRegexsMap = {
    			creditCard: String.raw`\b(?:4[0-9]{12}(?:[0-9]{3})?|(?:5[1-5][0-9]{2}|222[1-9]|22[3-9][0-9]|2[3-6][0-9]{2}|27[01][0-9]|2720)[0-9]{12}|3[47][0-9]{13}|3(?:0[0-5]|[68][0-9])[0-9]{11}|6(?:011|5[0-9]{2})[0-9]{12}|(?:2131|1800|35\d{3})\d{11})\b`,
    			email: String.raw`\b[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}\b`,
    			phone: String.raw`\b07\d{9}\b`,
    		};

    		for (const kind in sensitiveRegexsMap) {
    			const sensitiveRegex = new RegExp(sensitiveRegexsMap[kind], "ig");
    			const match = await sensitiveRegex.test(text);
    			if (match) {
    				//데이터 침해 경고
    				await postDataBreach(request);
    				//신용 카드 경우 블록과 응답,
    				//그렇지 않으면 `*`와 민감한 텍스트를 교체
    				return kind === "creditCard"
    					? new Response(kind + " found\nForbidden\n", {
    							status: 403,
    							statusText: "Forbidden",
    						})
    					: new Response(text.replace(sensitiveRegex, "**********"), response);
    			}
    		}
    		return new Response(text, response);
    	},
    } satisfies ExportedHandler;
    ```
  </TabItem>

  <TabItem label="Python" icon="seti:python">
    ```py
    import re
    from workers import WorkerEntrypoint
    from datetime import datetime
    from js import Response, fetch, JSON, Headers

    #웹훅 서버에 게시하여 데이터 침해 경고
    async def post_data_breach(request):
        some_hook_server = "https://webhook.flow-wolf.io/hook"
        headers = Headers.new({"content-type": "application/json"}.items())
        body = JSON.stringify({
          "ip": request.headers["cf-connecting-ip"],
          "time": datetime.now(),
          "request": request,
        })
        return await fetch(some_hook_server, method="POST", headers=headers, body=body)

    class Default(WorkerEntrypoint):
        async def fetch(self, request):
    		debug = True

    		#일반 표현으로 개인 데이터를 정의합니다.
    		#신용 카드 데이터 및 스트립 경우 블록과 응답
    		#응답에서 이메일 및 전화 번호.
    		#실행은 MIME 유형 "text/*"로 제한됩니다.
    		response = await fetch(request)

    		#응답이 텍스트가없는 경우 반환 기원 응답
    		content_type = response.headers["content-type"] or ""
    		if "text" not in content_type:
    			return response

    		text = await response.text()
    		#디버깅이 이메일과의 응답을 대체 할 때
    		text = text.replace("You may use this", "me@example.com may use this") if debug else text

    		sensitive_regex = [
    		("credit_card",
    		r'\b(?:4[0-9]{12}(?:[0-9]{3})?|(?:5[1-5][0-9]{2}|222[1-9]|22[3-9][0-9]|2[3-6][0-9]{2}|27[01][0-9]|2720)[0-9]{12}|3[47][0-9]{13}|3(?:0[0-5]|[68][0-9])[0-9]{11}|6(?:011|5[0-9]{2})[0-9]{12}|(?:2131|1800|35\d{3})\d{11})\b'),
    		("email", r'\b[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}\b'),
    		("phone", r'\b07\d{9}\b'),
    		]
    		for (kind, regex) in sensitive_regex:
    			match = re.search(regex, text, flags=re.IGNORECASE)
    			if match:
    				#데이터 침해 경고
    				await post_data_breach(request)
    				#신용 카드가 아닌 경우 블록과 응답, 그렇지 않으면 `*`S와 민감한 텍스트를 대체
    				card_resp = Response.new(kind + " found\nForbidden\n", status=403,statusText="Forbidden")
    				sensitive_resp = Response.new(re.sub(regex, "*"*10, text, flags=re.IGNORECASE), response)
    				return card_resp if kind == "credit_card" else  sensitive_resp

    		return Response.new(text, response)
    ```
  </TabItem>

  <TabItem label="호노" icon="seti:typescript">
    ```ts
    import { Hono } from 'hono';

    const app = new Hono();

    //제품 설명
    const DEBUG = true;
    const SOME_HOOK_SERVER = "https://webhook.flow-wolf.io/hook";

    //민감한 데이터 패턴 정의
    const sensitiveRegexsMap = {
      creditCard: String.raw`\b(?:4[0-9]{12}(?:[0-9]{3})?|(?:5[1-5][0-9]{2}|222[1-9]|22[3-9][0-9]|2[3-6][0-9]{2}|27[01][0-9]|2720)[0-9]{12}|3[47][0-9]{13}|3(?:0[0-5]|[68][0-9])[0-9]{11}|6(?:011|5[0-9]{2})[0-9]{12}|(?:2131|1800|35\d{3})\d{11})\b`,
      email: String.raw`\b[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}\b`,
      phone: String.raw`\b07\d{9}\b`,
    };

    /** 이름
    * 웹훅 서버에 게시하여 데이터 침해를 경고*/
    async function postDataBreach(request: Request) {
      return await fetch(SOME_HOOK_SERVER, {
        method: "POST",
        headers: {
          "content-type": "application/json;charset=UTF-8",
        },
        body: JSON.stringify({
          ip: request.headers.get("cf-connecting-ip"),
          time: Date.now(),
          request: request,
        }),
      });
    }

    //데이터 손실 방지를 처리하는 Main Middleware
    app.use('*', async (c) => {
      //원래 응답을 Fetch
      const response = await fetch(c.req.raw);

      //응답이 텍스트가없는 경우 반환 기원 응답
      const contentType = response.headers.get("content-type") || "";
      if (!contentType.toLowerCase().includes("text/")) {
        return response;
      }

      //응답 텍스트 받기
      let text = await response.text();

      //디버깅 할 때, 이메일과의 응답을 대체
      text = DEBUG
        ? text.replace("You may use this", "me@example.com may use this")
        : text;

      //민감한 데이터 확인
      for (const kind in sensitiveRegexsMap) {
        const sensitiveRegex = new RegExp(sensitiveRegexsMap[kind], "ig");
        const match = sensitiveRegex.test(text);

        if (match) {
          //데이터 침해 경고
          await postDataBreach(c.req.raw);

          //신용 카드가 아닌 경우 블록과 응답, 그렇지 않으면 `*`S와 민감한 텍스트를 대체
          if (kind === "creditCard") {
            return c.text(`${kind} found\nForbidden\n`, 403);
          } else {
            return new Response(text.replace(sensitiveRegex, "**********"), {
              status: response.status,
              statusText: response.statusText,
              headers: response.headers,
            });
          }
        }
      }

      //수정된 응답을 반환
      return new Response(text, {
        status: response.status,
        statusText: response.statusText,
        headers: response.headers,
      });
    });

    export default app;
    ```
  </TabItem>
</Tabs>
