---
summary: >-
  Send a request to a remote server, read HTML from the response, and serve that
  HTML.
tags:
  - JavaScript
  - TypeScript
  - Python
preview:
  - true
pcx_content_type: example
title: HTML 파일
reviewed: 2024-01-11T00:00:00.000Z
sidebar:
  order: 3
description: 원격 서버에 요청을 보내 응답에서 HTML을 읽고 HTML을 제공합니다.
translation_locale: ko-KR
translation_source_html: 'https://developers.cloudflare.com/workers/examples/fetch-html/'
translation_source_md: 'https://developers.cloudflare.com/workers/examples/fetch-html/index.md'
translation_source_kind: repo-mdx
translation_snapshot: '2026-03-13'
translation_upstream_ref: 66ee2dcd3715f1ca46e70954a3cda63f6cbebc75

---
get started를 신속히 원한다면 아래 버튼을 클릭하십시오.

[![Cloudflare에 배포](https://deploy.workers.cloudflare.com/button)](https://deploy.workers.cloudflare.com/?url=https://github.com/cloudflare/docs-examples/tree/main/workers/fetch-html)

GitHub 계정에서 저장소를 생성하고 Cloudflare Workers에 응용 프로그램을 배포합니다.

import { Render, TabItem, Tabs } from "~/components";

<Tabs syncKey="workersExamples">
  <TabItem label="JavaScript" icon="seti:javascript">
    <Render file="fetch-html-example-js" product="workers" />
  </TabItem>

  <TabItem label="TypeScript" icon="seti:typescript">
    ```ts
    export default {
    	async fetch(request: Request): Promise<Response> {
    		/** 이름
    * `remote`를 호스트로 교체하여 요청을 보내려면*/
    		const remote = "https://example.com";

    		return await fetch(remote, request);
    	},
    };
    ```
  </TabItem>

  <TabItem label="Python" icon="seti:python">
    ```py
    from workers import WorkerEntrypoint
    from js import fetch

    class Default(WorkerEntrypoint):
        async def fetch(self, request):
            #`remote`를 호스트로 바꾸어 요청을 보내려면
            remote = "https://example.com"
            return await fetch(remote, request)
    ```
  </TabItem>

  <TabItem label="호노" icon="seti:typescript">
    ```ts
    import { Hono } from "hono";

    const app = new Hono();

    app.all("*", async (c) => {
    	/** 이름
    * `remote`를 호스트로 교체하여 요청을 보내려면*/
    	const remote = "https://example.com";

    	//원격 서버에 요청을 전달
    	return await fetch(remote, c.req.raw);
    });

    export default app;
    ```
  </TabItem>
</Tabs>
