---
summary: >-
  Return a response based on the incoming request's URL, HTTP method, User
  Agent, IP address, ASN or device type.
tags:
  - Middleware
  - JavaScript
  - TypeScript
  - Python
pcx_content_type: example
title: 상태 응답
sidebar:
  order: 1001
description: '수신 요청의 URL, HTTP 방법, 사용자 에이전트, IP 주소, ASN 또는 장치 유형에 따라 응답을 반환합니다.'
reviewed: 2022-02-25T00:00:00.000Z
translation_locale: ko-KR
translation_source_html: 'https://developers.cloudflare.com/workers/examples/conditional-response/'
translation_source_md: >-
  https://developers.cloudflare.com/workers/examples/conditional-response/index.md
translation_source_kind: repo-mdx
translation_snapshot: '2026-03-13'
translation_upstream_ref: 66ee2dcd3715f1ca46e70954a3cda63f6cbebc75

---
get started를 신속히 원한다면 아래 버튼을 클릭하십시오.

[![Cloudflare에 배포](https://deploy.workers.cloudflare.com/button)](https://deploy.workers.cloudflare.com/?url=https://github.com/cloudflare/docs-examples/tree/main/workers/conditional-response)

GitHub 계정의 저장소를 만들고 Cloudflare Workers에 애플리케이션을 배포합니다.

import { TabItem, Tabs } from "~/components";

<Tabs syncKey="workersExamples">
  <TabItem label="모델 번호: JavaScript" icon="seti:javascript">
    ```js
    export default {
    	async fetch(request) {
    		const BLOCKED_HOSTNAMES = ["nope.mywebsite.com", "bye.website.com"];
    		//URL의 호스트 이름에 따라 새로운 응답을 반환
    		const url = new URL(request.url);
    		if (BLOCKED_HOSTNAMES.includes(url.hostname)) {
    			return new Response("Blocked Host", { status: 403 });
    		}
    		//URL의 파일 확장을 기반으로 .doc 또는 .xml에서 차단 경로
    		const forbiddenExtRegExp = new RegExp(/\.(doc|xml)$/);
    		if (forbiddenExtRegExp.test(url.pathname)) {
    			return new Response("Blocked Extension", { status: 403 });
    		}
    		//HTTP 방법
    		if (request.method === "POST") {
    			return new Response("Response for POST");
    		}
    		//사용자 에이전트
    		const userAgent = request.headers.get("User-Agent") || "";
    		if (userAgent.includes("bot")) {
    			return new Response("Block User Agent containing bot", { status: 403 });
    		}
    		//고객의 IP 주소
    		const clientIP = request.headers.get("CF-Connecting-IP");
    		if (clientIP === "1.2.3.4") {
    			return new Response("Block the IP 1.2.3.4", { status: 403 });
    		}
    		//ASN에서
    		if (request.cf && request.cf.asn == 64512) {
    			return new Response("Block the ASN 64512 response");
    		}
    		//장치 유형
    		//엔터프라이즈 "CF-Device-Type Header" 영역 설정 또는
    		//Page Rule with "Cache By Device Type" 설정 적용.
    		const device = request.headers.get("CF-Device-Type");
    		if (device === "mobile") {
    			return Response.redirect("https://mobile.example.com");
    		}
    		console.error(
    			"Getting Client's IP address, device type, and ASN are not supported in playground. Must test on a live worker",
    		);
    		return fetch(request);
    	},
    };
    ```
  </TabItem>

  <TabItem label="모델 번호: TypeScript" icon="seti:typescript">
    ```ts
    export default {
    	async fetch(request): Promise<Response> {
    		const BLOCKED_HOSTNAMES = ["nope.mywebsite.com", "bye.website.com"];
    		//URL의 호스트 이름에 따라 새로운 응답을 반환
    		const url = new URL(request.url);
    		if (BLOCKED_HOSTNAMES.includes(url.hostname)) {
    			return new Response("Blocked Host", { status: 403 });
    		}
    		//URL의 파일 확장을 기반으로 .doc 또는 .xml에서 차단 경로
    		const forbiddenExtRegExp = new RegExp(/\.(doc|xml)$/);
    		if (forbiddenExtRegExp.test(url.pathname)) {
    			return new Response("Blocked Extension", { status: 403 });
    		}
    		//HTTP 방법
    		if (request.method === "POST") {
    			return new Response("Response for POST");
    		}
    		//사용자 에이전트
    		const userAgent = request.headers.get("User-Agent") || "";
    		if (userAgent.includes("bot")) {
    			return new Response("Block User Agent containing bot", { status: 403 });
    		}
    		//고객의 IP 주소
    		const clientIP = request.headers.get("CF-Connecting-IP");
    		if (clientIP === "1.2.3.4") {
    			return new Response("Block the IP 1.2.3.4", { status: 403 });
    		}
    		//ASN에서
    		if (request.cf && request.cf.asn == 64512) {
    			return new Response("Block the ASN 64512 response");
    		}
    		//장치 유형
    		//엔터프라이즈 "CF-Device-Type Header" 영역 설정 또는
    		//Page Rule with "Cache By Device Type" 설정 적용.
    		const device = request.headers.get("CF-Device-Type");
    		if (device === "mobile") {
    			return Response.redirect("https://mobile.example.com");
    		}
    		console.error(
    			"Getting Client's IP address, device type, and ASN are not supported in playground. Must test on a live worker",
    		);
    		return fetch(request);
    	},
    } satisfies ExportedHandler;
    ```
  </TabItem>

  <TabItem label="모델 번호: Python" icon="seti:python">
    ```py
    import re
    from workers import WorkerEntrypoint, Response, fetch
    from urllib.parse import urlparse

    class Default(WorkerEntrypoint):
        async def fetch(self, request):
            blocked_hostnames = ["nope.mywebsite.com", "bye.website.com"]
            url = urlparse(request.url)

            #hostname에 블록
            if url.hostname in blocked_hostnames:
                return Response("Blocked Host", status=403)

            #.doc 또는 .xml에서 끝나는 경로
            if re.search(r'\.(doc|xml)$', url.path):
                return Response("Blocked Extension", status=403)

            #HTTP 방법
            if "POST" in request.method:
                return Response("Response for POST")

            #사용자 에이전트
            user_agent = request.headers["User-Agent"] or ""
            if "bot" in user_agent:
                return Response("Block User Agent containing bot", status=403)

            #고객의 IP 주소
            client_ip = request.headers["CF-Connecting-IP"]
            if client_ip == "1.2.3.4":
                return Response("Block the IP 1.2.3.4", status=403)

            #ASN에서
            if request.cf and request.cf.asn == 64512:
                return Response("Block the ASN 64512 response")

            #장치 유형
            #엔터프라이즈 "CF-Device-Type Header" 영역 설정 또는
            #Page Rule with "Cache By Device Type" 설정 적용.
            device = request.headers["CF-Device-Type"]
            if device == "mobile":
                return Response.redirect("https://mobile.example.com")

            return fetch(request)
    ```
  </TabItem>

  <TabItem label="호노" icon="seti:typescript">
    ```ts
    import { Hono } from "hono";
    import { HTTPException } from "hono/http-exception";

    const app = new Hono();

    //주요 핸들러에 도달하기 전에 모든 조건을 처리하는 Middleware
    app.use("*", async (c, next) => {
    	const request = c.req.raw;
    	const BLOCKED_HOSTNAMES = ["nope.mywebsite.com", "bye.website.com"];
    	const hostname = new URL(c.req.url)?.hostname;

    	//URL의 호스트 이름에 따라 새로운 응답을 반환
    	if (BLOCKED_HOSTNAMES.includes(hostname)) {
    		return c.text("Blocked Host", 403);
    	}

    	//URL의 파일 확장을 기반으로 .doc 또는 .xml에서 차단 경로
    	const forbiddenExtRegExp = new RegExp(/\.(doc|xml)$/);
    	if (forbiddenExtRegExp.test(c.req.pathname)) {
    		return c.text("Blocked Extension", 403);
    	}

    	//사용자 에이전트
    	const userAgent = c.req.header("User-Agent") || "";
    	if (userAgent.includes("bot")) {
    		return c.text("Block User Agent containing bot", 403);
    	}

    	//고객의 IP 주소
    	const clientIP = c.req.header("CF-Connecting-IP");
    	if (clientIP === "1.2.3.4") {
    		return c.text("Block the IP 1.2.3.4", 403);
    	}

    	//ASN에서
    	if (request.cf && request.cf.asn === 64512) {
    		return c.text("Block the ASN 64512 response");
    	}

    	//장치 유형
    	//엔터프라이즈 "CF-Device-Type Header" 영역 설정 또는
    	//Page Rule with "Cache By Device Type" 설정 적용.
    	const device = c.req.header("CF-Device-Type");
    	if (device === "mobile") {
    		return c.redirect("https://mobile.example.com");
    	}

    	//다음 핸들러로 계속
    	await next();
    });

    //POST 요청을 다르게 처리
    app.post("*", (c) => {
    	return c.text("Response for POST");
    });

    //다른 방법의 기본 핸들러
    app.get("*", async (c) => {
    	console.error(
    		"Getting Client's IP address, device type, and ASN are not supported in playground. Must test on a live worker",
    	);

    	//자주 묻는 질문
    	return fetch(c.req.raw);
    });

    export default app;
    ```
  </TabItem>
</Tabs>
