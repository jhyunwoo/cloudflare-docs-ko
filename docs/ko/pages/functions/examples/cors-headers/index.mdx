---
summary: A Pages Functions for appending CORS headers.
tags:
  - Headers
pcx_content_type: configuration
title: CORS 헤더 추가
sidebar:
  order: 1002
description: Pages CORS 헤더를 승인하는 기능.
translation_locale: ko-KR
translation_source_html: 'https://developers.cloudflare.com/pages/functions/examples/cors-headers/'
translation_source_md: >-
  https://developers.cloudflare.com/pages/functions/examples/cors-headers/index.md
translation_source_kind: repo-mdx
translation_snapshot: '2026-03-13'
translation_upstream_ref: 66ee2dcd3715f1ca46e70954a3cda63f6cbebc75

---
이 예제는 Cloudflare PagesXQ에서 스니펫입니다. 템플릿 repo.

```ts
//OPTIONS 방법 응답
export const onRequestOptions: PagesFunction = async () => {
	return new Response(null, {
		status: 204,
		headers: {
			"Access-Control-Allow-Origin": "*",
			"Access-Control-Allow-Headers": "*",
			"Access-Control-Allow-Methods": "GET, OPTIONS",
			"Access-Control-Max-Age": "86400",
		},
	});
};

//CORS를 모든 /api 응답 설정
export const onRequest: PagesFunction = async (context) => {
	const response = await context.next();
	response.headers.set("Access-Control-Allow-Origin", "*");
	response.headers.set("Access-Control-Max-Age", "86400");
	return response;
};
```
