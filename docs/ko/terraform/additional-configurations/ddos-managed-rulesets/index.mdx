---
title: Terraform을 이용한 DDoS 관리 규정
pcx_content_type: how-to
sidebar:
  order: 3
  label: DDoS 관리 규정
head:
  - tag: title
    content: Terraform을 이용한 DDoS 관리 규정
translation_locale: ko-KR
translation_source_html: >-
  https://developers.cloudflare.com/terraform/additional-configurations/ddos-managed-rulesets/
translation_source_md: >-
  https://developers.cloudflare.com/terraform/additional-configurations/ddos-managed-rulesets/index.md
translation_source_kind: repo-mdx
translation_snapshot: '2026-03-13'
translation_upstream_ref: 66ee2dcd3715f1ca46e70954a3cda63f6cbebc75

---
import { Render, RuleID } from "~/components";

이 페이지는 구성 예제를 제공합니다.[DDoS 관리 규정](/ddos-protection/managed-rulesets/)Terraform을 사용하여 영역 또는 계정에서. 그것은 뒤에 오는 윤곽을 포함합니다:

- [예: HTTP DDoS 공격 보호 구성](#example-configure-http-ddos-attack-protection)
- [예: Network-layer DDoS 공격 보호 구성](#example-configure-network-layer-ddos-attack-protection)
- [사용 케이스: 큰 HTTP DDoS 공격을 시작 하 고 추적 트래픽을 모니터링](#use-case-mitigate-large-http-ddos-attacks-and-monitor-flagged-traffic)

DDoS 관리 규정은 항상 활성화됩니다. Cloudflare 서비스에 따라 행동을 조정할 수 있습니다.

Cloudflare API를 사용하는 경우 다음 리소스를 참조하십시오.

- [API를 통해 HTTP DDoS 공격 보호 구성](/ddos-protection/managed-rulesets/http/http-overrides/configure-api/)
- [API를 통해 Network-layer DDoS 공격 보호 구성](/ddos-protection/managed-rulesets/network/network-overrides/configure-api/)

Rulesets API를 사용하여 배포 및 구성 규칙에 대한 자세한 내용은 참조[관리 규칙과 함께 일](/ruleset-engine/managed-rulesets/)Ruleset Engine 문서에서.

## 시작하기 전에

### 필요한 계정, 영역 및 관리되는 규칙 ID를 얻으십시오.

<Render file="find-ids-managed-rulesets" product="terraform" />

### (선택 사항) 스크래치에서 시작하려면 기존 규칙 삭제

<Render file="import-delete-existing-rulesets" product="terraform" />

***

## 예: HTTP DDoS 공격 보호 구성

이 예제는[HTTP DDoS 공격 보호](/ddos-protection/managed-rulesets/http/)Terraform을 사용하여 영역에 대한 관리 규칙.

<Render file="v4-code-snippets" product="terraform" />

```tf
resource "cloudflare_ruleset" "zone_level_http_ddos_config" {
  zone_id     = "<ZONE_ID>"
  name        = "HTTP DDoS Attack Protection entry point ruleset"
  description = ""
  kind        = "zone"
  phase       = "ddos_l7"

  rules {
    action = "execute"
    action_parameters {
      # Cloudflare L7 DDoS Attack Protection Ruleset
      id = "4d21379b4f9f4bb088e0729962c8b3cf"
      overrides {
        action = "block"
        sensitivity_level = "default"
        rules {
          # Adaptive DDoS Protection based on Locations (Available only to Enterprise zones with Advanced DDoS service)
          id = "a8c6333711ff4b0a81371d1c444be2c3"
          sensitivity_level = "default"
          action = "managed_challenge"
        }
        rules {
          # Adaptive DDoS Protection based on User-Agents (Available only to Enterprise zones with Advanced DDoS service)
          id = "7709d496081e458899c1e3a6e4fe8e55"
          sensitivity_level = "default"
          action = "managed_challenge"
        }
        rules {
          # HTTP requests causing a high number of origin errors.
          id = "dd42da7baabe4e518eaf11c393596a9d"
          sensitivity_level = "default"
          action = "managed_challenge"
        }
      }
    }
    expression = "true"
    description = "Zone-wide HTTP DDoS Override"
    enabled = true
  }
}
```

HTTP DDoS 공격 보호에 대한 자세한 내용은 참조[HTTP DDoS 공격 보호 관리 규정](/ddos-protection/managed-rulesets/http/).

## 예: Network-layer DDoS 공격 보호 구성

이 예제는[Network-layer DDoS 공격 보호](/ddos-protection/managed-rulesets/network/)Terraform을 사용하여 계정 관리 규칙, ID와 규칙의 감도 수준을 변경<RuleID id="599dab0942ff4898ac1b7797e954e98b" />으로`low`오버라이드를 사용하여.

:::카ution\[중요]

- Enterprise 플랜에서 Magic Transit 및 Spectrum 고객만 구성할 수 있습니다.
- 이 관리된 규칙은 계정 수준에서 overrides만 지원합니다.

:::

<Render file="v4-code-snippets" product="terraform" />

```tf
resource "cloudflare_ruleset" "account_level_network_ddos_config" {
  account_id  = "<ACCOUNT_ID>"
  name        = "Network-layer DDoS Attack Protection entry point ruleset"
  description = ""
  kind        = "root"
  phase       = "ddos_l4"

  rules {
    ref         = "override_l7_ddos_ruleset_dst_ip"
    description = "Override the HTTP DDoS Attack Protection managed ruleset"
    expression  = "ip.dst in { 192.0.2.0/24 }"
    action      = "execute"
    action_parameters {
      # Cloudflare L3/4 DDoS Attack Protection Ruleset
      id = "3b64149bfa6e4220bbbc2bd6db589552"
      overrides {
        rules {
          # Rule: Generic high-volume UDP traffic flows.
          id                = "599dab0942ff4898ac1b7797e954e98b"
          sensitivity_level = "low"
        }
      }
    }
  }
}
```

Network-layer DDoS 공격 보호에 대한 자세한 내용은 여기를 참조하십시오.[Network-layer DDoS 공격 보호 관리 규정](/ddos-protection/managed-rulesets/network/).

***

## 사용 케이스: 큰 HTTP DDoS 공격을 시작 하 고 추적 트래픽을 모니터링

다음 예에서 고객은 false 긍정적 인 것에 대해 우려하지만 큰 HTTP DDoS 공격에 대한 보호를 얻기를 원합니다. 두 가지 규칙, 각각 두 개의 overrides 포함, 그들의[HTTP DDoS 보호](/ddos-protection/managed-rulesets/http/)구성에는 다음과 같은 동작이 있습니다:

1. 규칙을 구성하여 큰 HTTP DDoS 공격을 Mitigate*이름 \** [감도 수준](/ddos-protection/managed-rulesets/http/override-parameters/#sensitivity-level)·*제품정보*활동.
2. 디도스 보호 시스템에 의해 전파되는 모니터링 트래픽은 기본 감도 수준으로 규칙을 구성함으로써 (*사이트맵*·*로그인*활동.

규칙의 순서는 중요합니다: 가장 높은 감도 수준을 가진 규칙은 가장 낮은 감도 수준으로 규칙 후에, 그렇지 않으면 평가되지 않을 것입니다.

:::caution\[중요한 고려사항]

- DDoS 공격 완화가 진행될 때, Cloudflare는 규칙 순서를 확인하고 표현과 감도 수준 둘 다 일치하는 첫번째 것을 적용합니다.
- 규칙이 순서대로 평가되고 첫 번째는 표현의 조건과 감도 수준이 적용 될 것이며, 기존 규칙을 편집하고 재주문 할 때주의를 기울입니다. Block to Log의 규칙을 변경하면 공격 트래픽이 웹 속성에 도달 할 수 있습니다.
- Overrides는 관리 규칙에 읽기 전용 규칙에 영향을 미치지 않습니다.

:::

<Render file="v4-code-snippets" product="terraform" />

```tf
variable "zone_id" {
  default = "<ZONE_ID>"
}

resource "cloudflare_ruleset" "zone_level_http_ddos_config" {
  zone_id     = var.zone_id
  name        = "HTTP DDoS - Terraform managed"
  description = ""
  kind        = "zone"
  phase       = "ddos_l7"

  # The resource configuration contains two rules:
  #  1. The first rule has the lowest sensitivity level (highest threshold)
  #     and it will block attacks.
  #  2. The second rule has a higher sensitivity level (lower threshold) and
  #     will only apply a Log action.
  #
  # In practice, evaluation stops whenever a rule matches both the expression
  # and the threshold, so the rule order is important:
  #   - When the traffic rate is below the (low) threshold of the default
  #     sensitivity level ('High'), no rules match (no action is applied).
  #   - When the traffic rate is between the thresholds of the 'Low' and
  #     default ('High') sensitivity levels, the first rule does not match,
  #     but the second rule does (traffic gets logged).
  #   - When the traffic rate goes above the (high) threshold of the 'Low'
  #     sensitivity level, the first rule matches (traffic gets blocked).
  #
  # The DDoS protection systems will still apply mitigation actions to incoming
  # traffic when rates exceed the threshold of the _Essentially Off_ sensitivity
  # level.

  rules {
    ref         = "l7_ddos_block_traffic_low_threshold"
    description = "At the low sensitivity threshold, block the traffic"
    expression  = "true"
    action      = "execute"
    action_parameters {
      # Cloudflare L7 DDoS Attack Protection Ruleset
      id = "4d21379b4f9f4bb088e0729962c8b3cf"
      overrides {
        rules {
          # Rule: HTTP requests from known botnet (signature #4).
          id                = "29d170ba2f004cc787b1ac272c9e04e7"
          sensitivity_level = "low"
          action            = "block"
        }
        rules {
          # Rule: HTTP requests with unusual HTTP headers or URI path (signature #16).
          id                = "60a48054bbcf4014ac63c44f1712a123"
          sensitivity_level = "low"
          action            = "block"
        }
      }
    }
  }

  rules {
    ref         = "l7_ddos_log_default_threshold"
    description = "At the default sensitivity threshold, log to see if any legitimate traffic gets caught"
    expression  = "true"
    action      = "execute"
    action_parameters {
      # Cloudflare L7 DDoS Attack Protection Ruleset
      id = "4d21379b4f9f4bb088e0729962c8b3cf"
      overrides {
        rules {
          # Rule: HTTP requests from known botnet (signature #4).
          id                = "29d170ba2f004cc787b1ac272c9e04e7"
          sensitivity_level = "default"
          action            = "log"
        }
        rules {
          # Rule: HTTP requests with unusual HTTP headers or URI path (signature #16).
          id                = "60a48054bbcf4014ac63c44f1712a123"
          sensitivity_level = "default"
          action            = "log"
        }
      }
    }
  }
}
```
