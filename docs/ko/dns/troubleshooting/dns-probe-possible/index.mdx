---
title: DNS_PROBE_POSSIBLE
pcx_content_type: troubleshooting
sidebar:
  order: 5
head:
  - tag: title
    content: DNS_PROBE_POSSIBLE 오류 해결
---

import { Render } from "~/components";

<Render
	file="dns-errors-ts-intro"
	product="dns"
	params={{ one: "DNS_PROBE_POSSIBLE" }}
/>

## 배경

`DNS_PROBE_POSSIBLE`은 resolver가 요청된 hostname에 대한 DNS 레코드를 찾을 수 없었다는 의미입니다.

방문자가 네트워크 문제나 로컬 DNS 문제 때문에 이 오류 또는 Safari, Edge, Firefox의 유사한 메시지를 만나는 경우도 있지만, Cloudflare의 DNS 레코드 문제를 가리킬 수도 있습니다.

## 가능한 해결 방법

<Render
	file="dns-errors-ts-action"
	product="dns"
	params={{ one: "DNS_PROBE_POSSIBLE" }}
/>
