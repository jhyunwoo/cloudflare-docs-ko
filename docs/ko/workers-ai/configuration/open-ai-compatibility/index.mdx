---
pcx_content_type: configuration
title: OpenAI 호환 API 엔드포인트
sidebar:
  order: 3
translation_locale: ko-KR
translation_source_html: >-
  https://developers.cloudflare.com/workers-ai/configuration/open-ai-compatibility/
translation_source_md: >-
  https://developers.cloudflare.com/workers-ai/configuration/open-ai-compatibility/index.md
translation_source_kind: repo-mdx
translation_snapshot: '2026-03-13'
translation_upstream_ref: 66ee2dcd3715f1ca46e70954a3cda63f6cbebc75

---
import { Render } from "~/components";

<Render file="openai-compatibility" product="workers-ai" />

<br />

## 제품 정보

### Workers AI

정상적으로, Workers AI는 당신이 cURL 엔드포인트에 있는 모형 이름을 지정하는 것을 요구합니다 또는 안에`env.AI.run`기능.

OpenAI 호환 엔드포인트를 사용하면 레버리지 할 수 있습니다.[openai 양극 sdk](https://github.com/openai/openai-node)Workers AIXQ로 호출합니다. Workers AI를 사용하여 기본 URL과 모델 이름을 변경할 수 있습니다.

```js title="OpenAI SDK Example"
import OpenAI from "openai";

const openai = new OpenAI({
	apiKey: env.CLOUDFLARE_API_KEY,
	baseURL: `https://api.cloudflare.com/client/v4/accounts/${env.CLOUDFLARE_ACCOUNT_ID}/ai/v1`,
});

//채팅 완료
const chatCompletion = await openai.chat.completions.create({
	messages: [{ role: "user", content: "Make some robot noises" }],
	model: "@cf/meta/llama-3.1-8b-instruct",
});

//사용 응답
const response = await openai.responses.create({
	model: "@cf/openai/gpt-oss-120b",
	input: "Talk to me about open source",
});

const embeddings = await openai.embeddings.create({
	model: "@cf/baai/bge-large-en-v1.5",
	input: "I love matcha",
});
```

```bash title="cURL example"
curl --request POST \
  --url https://api.cloudflare.com/client/v4/accounts/{account_id}/ai/v1/chat/completions \
  --header "Authorization: Bearer {api_token}" \
  --header "Content-Type: application/json" \
  --data '
    {
      "model": "@cf/meta/llama-3.1-8b-instruct",
      "messages": [
        {
          "role": "user",
          "content": "how to build a wooden spoon in 3 short steps? give as short as answer as possible"
        }
      ]
    }
'
```

### AI 게이트웨이

이 엔드포인트도 호환됩니다.[AI 게이트웨이](/ai-gateway/usage/providers/workersai/#openai-compatible-endpoints).
