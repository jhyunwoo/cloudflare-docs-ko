---
pcx_content_type: example
title: fetch() 핸들러 사용
tags:
  - AI
sidebar:
  order: 4
description: >-
  Cloudflare QXWorkers AI의 핸들러를 사용하여 API 통화를 수행 할 수 있도록 LLM을 사용하여 기능 호출을 사용하여 5
  일간의 일기 예보를 검색하십시오.
reviewed: 2024-07-09T00:00:00.000Z
translation_locale: ko-KR
translation_source_html: >-
  https://developers.cloudflare.com/workers-ai/features/function-calling/embedded/examples/fetch/
translation_source_md: >-
  https://developers.cloudflare.com/workers-ai/features/function-calling/embedded/examples/fetch/index.md
translation_source_kind: repo-mdx
translation_snapshot: '2026-03-13'
translation_upstream_ref: 66ee2dcd3715f1ca46e70954a3cda63f6cbebc75

---
아주 일반적인 사용 케이스는 함수 호출을 통해 API 호출을 수행 할 수있는 능력을 가진 LLM을 제공합니다.

이 예에서 LLM은 다음 5 일 동안의 일기 예보를 검색합니다.
이렇게하려면`getWeather`함수는 LLM에 도구로 전달되는 것을 정의한다.

더 보기`getWeather`함수는 요청에서 사용자의 위치를 추출하고 Workers를 통해 외부 날씨 API를 호출[`Fetch API`](/workers/runtime-apis/fetch/)그리고 결과를 반환합니다.

```ts title="Embedded function calling example with fetch()"
import { runWithTools } from '@cloudflare/ai-utils';

type Env = {
	AI: Ai;
};

export default {
	async fetch(request, env, ctx) {
		//기능 정의
		const getWeather = async (args: { numDays: number }) => {
			const { numDays } = args;
      //위치는 요청에 따라 추출됩니다.
      //https://developers.cloudflare.com/workers/runtime-apis/request/#incomingrequestcfproperties
      const lat = request.cf?.latitude
      const long = request.cf?.longitude

      //외부 API 호출의 Interpolate 값
			const response = await fetch(
				`https://api.open-meteo.com/v1/forecast?latitude=${lat}&longitude=${long}&daily=temperature_2m_max,precipitation_sum&timezone=GMT&forecast_days=${numDays}`
			);
			return response.text();
		};
		//기능 호출과 AI inference 실행
		const response = await runWithTools(
			env.AI,
			//기능 호출 지원 모델
			'@hf/nousresearch/hermes-2-pro-mistral-7b',
			{
				//이름 *
				messages: [
					{
						role: 'user',
						content: 'What the weather like the next 5 days? Respond as text',
					},
				],
				//AI 모델을 활용할 수 있는 도구의 정의
				tools: [
					{
						name: 'getWeather',
						description: 'Get the weather for the next [numDays] days',
						parameters: {
							type: 'object',
							properties: {
								numDays: { type: 'numDays', description: 'number of days for the weather forecast' },
							},
							required: ['numDays'],
						},
						//이전에 정의된 기능에 reference
						function: getWeather,
					},
				],
			}
		);
		return new Response(JSON.stringify(response));
	},
} satisfies ExportedHandler<Env>;

```
