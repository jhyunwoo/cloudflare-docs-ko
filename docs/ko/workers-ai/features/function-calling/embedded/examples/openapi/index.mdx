---
pcx_content_type: navigation
title: OpenAPI Spec을 기반으로 하는 도구
tags:
  - AI
sidebar:
  order: 5
translation_locale: ko-KR
translation_source_html: >-
  https://developers.cloudflare.com/workers-ai/features/function-calling/embedded/examples/openapi/
translation_source_md: >-
  https://developers.cloudflare.com/workers-ai/features/function-calling/embedded/examples/openapi/index.md
translation_source_kind: repo-mdx
translation_snapshot: '2026-03-13'
translation_upstream_ref: 66ee2dcd3715f1ca46e70954a3cda63f6cbebc75

---
Oftentimes API는 정의되고 문서화됩니다.[OpenAPI 사양](https://swagger.io/specification/). Cloudflare`ai-utils`패키지의`createToolsFromOpenAPISpec`함수는 OpenAPI spec에서 도구로 만듭니다. LLM은 그 후 프롬프트를 달성할 수 있습니다.

이 예에서 LLM은 Github 사용자를 설명합니다. Github의 API와 OpenAPI 사양을 기반으로합니다.

```ts title="Embedded function calling example from OpenAPI Spec"
import { createToolsFromOpenAPISpec, runWithTools } from '@cloudflare/ai-utils';

type Env = {
	AI: Ai;
};

const APP_NAME = 'cf-fn-calling-example-app';

export default {
	async fetch(request, env, ctx) {
		const toolsFromOpenAPISpec = [
			//OpenAPI 사양 링크 또는 컨텐츠를 직접 전달할 수 있습니다.
			...(await createToolsFromOpenAPISpec(
				'https://gist.githubusercontent.com/mchenco/fd8f20c8f06d50af40b94b0671273dc1/raw/f9d4b5cd5944cc32d6b34cad0406d96fd3acaca6/partial_api.github.com.json',
				{
					overrides: [
						{
							matcher: ({ url }) => {
								return url.hostname === 'api.github.com';
							},
							//*.github.com에 대한 모든 요청을 위해, 우리는 User-Agent를 추가해야합니다.
							values: {
								headers: {
									'User-Agent': APP_NAME,
								},
							},
						},
					],
				}
			)),
		];

		const response = await runWithTools(
			env.AI,
			'@hf/nousresearch/hermes-2-pro-mistral-7b',
			{
				messages: [
					{
						role: 'user',
						content: 'Who is cloudflare on Github and how many repos does the organization have?',
					},
				],
				tools: toolsFromOpenAPISpec,
			}
		);

		return new Response(JSON.stringify(response));
	},
} satisfies ExportedHandler<Env>;

```
