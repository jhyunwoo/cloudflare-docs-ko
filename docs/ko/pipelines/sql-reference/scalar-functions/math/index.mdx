---
title: 수학 기능
description: mathematical 작업에 대한 Scalar 기능
sidebar:
  order: 1
translation_locale: ko-KR
translation_source_html: >-
  https://developers.cloudflare.com/pipelines/sql-reference/scalar-functions/math/
translation_source_md: >-
  https://developers.cloudflare.com/pipelines/sql-reference/scalar-functions/math/index.md
translation_source_kind: repo-mdx
translation_snapshot: '2026-03-13'
translation_upstream_ref: 66ee2dcd3715f1ca46e70954a3cda63f6cbebc75

---
*Cloudflare Pipelines scalar 함수 구현은
[아파치 DataFusion](https://arrow.apache.org/datafusion/)(대략)[아로요](https://www.arroyo.dev/)) 그리고 이 문서는에서 파생됩니다
DataFusion 기능 참고 자료.*

## `abs`

숫자의 절대값을 반환합니다.

```
abs(numeric_expression)
```

**이름 \***

- **numeric 압축**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `acos`

아크 코신 또는 숫자의 반대 코신을 반환합니다.

```
acos(numeric_expression)
```

**이름 \***

- **numeric 압축**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `acosh`

지역 hyperbolic cosine 또는 숫자의 반전 hyperbolic cosine를 돌려줍니다.

```
acosh(numeric_expression)
```

**이름 \***

- **numeric 압축**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `asin`

번호의 아크 사인 또는 반대 사인을 반환합니다.

```
asin(numeric_expression)
```

**이름 \***

- **numeric 압축**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `asinh`

지역 hyperbolic sine 또는 숫자의 반전 hyperbolic 사인을 돌려줍니다.

```
asinh(numeric_expression)
```

**이름 \***

- **numeric 압축**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `atan`

숫자의 arc tangent 또는 inverse tangent를 반환합니다.

```
atan(numeric_expression)
```

**이름 \***

- **numeric 압축**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `atanh`

지역 hyperbolic tangent 또는 숫자의 역근한 tangent를 돌려줍니다.

```
atanh(numeric_expression)
```

**이름 \***

- **numeric 압축**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `atan2`

arc tangent 또는 inverse tangent를 반환합니다`expression_y / expression_x`.

```
atan2(expression_y, expression_x)
```

**이름 \***

- **본문내용**: 동작하는 첫번째 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.
- **파일 형식**: 두 번째 숫자 표현은 작동한다.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `cbrt`

숫자의 큐브 루트를 반환합니다.

```
cbrt(numeric_expression)
```

**이름 \***

- **numeric 압축**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `ceil`

가장 가까운 정수를 숫자와 동일하게 반환합니다.

```
ceil(numeric_expression)
```

**이름 \***

- **numeric 압축**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `cos`

번호를 반환합니다.

```
cos(numeric_expression)
```

**이름 \***

- **numeric 압축**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `cosh`

숫자의 hyperbolic 코신을 반환합니다.

```
cosh(numeric_expression)
```

**이름 \***

- **numeric 압축**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `degrees`

도에 반경을 변환합니다.

```
degrees(numeric_expression)
```

**이름 \***

- **numeric 압축**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `exp`

숫자의 기초 e exponential를 반환합니다.

```
exp(numeric_expression)
```

**이름 \***

- **numeric 압축**: exponent로 사용할 수 있는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `factorial`

공장 투어 반환 1 값이 2 미만인 경우

```
factorial(numeric_expression)
```

**이름 \***

- **numeric 압축**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `floor`

가장 가까운 정수를 넘거나 숫자와 동일하게 반환합니다.

```
floor(numeric_expression)
```

**이름 \***

- **numeric 압축**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `gcd`

가장 큰 일반적인 디바이저를 반환합니다.`expression_x`·`expression_y`. 둘 다 입력이 0인 경우에 반환 0.

```
gcd(expression_x, expression_y)
```

**이름 \***

- **파일 형식**: 동작하는 첫번째 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.
- **본문내용**: 두 번째 숫자 표현은 작동한다.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `isnan`

주어진 번호가 +NaN 또는 -NaN이라면 true를 반환합니다. 그렇지 않으면 false를 반환합니다.

```
isnan(numeric_expression)
```

**이름 \***

- **numeric 압축**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `iszero`

주어진 숫자가 +0.0 또는 -0.0이라면 true를 반환합니다. 그렇지 않으면 false를 반환합니다.

```
iszero(numeric_expression)
```

**이름 \***

- **numeric 압축**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `lcm`

가장 일반적인 여러 가지 반환`expression_x`·`expression_y`. 1개의 입력이 0인 경우에 반환 0.

```
lcm(expression_x, expression_y)
```

**이름 \***

- **파일 형식**: 동작하는 첫번째 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.
- **본문내용**: 두 번째 숫자 표현은 작동한다.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `ln`

숫자의 자연 통나무를 반환합니다.

```
ln(numeric_expression)
```

**이름 \***

- **numeric 압축**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `log`

숫자의 base-x logarithm를 반환합니다.
지정된베이스를 제공 할 수 있습니다. 또는 omitted 경우 숫자의 base-10을 걸립니다.

```
log(base, numeric_expression)
log(numeric_expression)
```

**이름 \***

- **기본 정보**: Base numeric 표현은 동작한다.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.
- **numeric 압축**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `log10`

숫자의 base-10 logarithm를 반환합니다.

```
log10(numeric_expression)
```

**이름 \***

- **numeric 압축**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `log2`

숫자의 base-2 logarithm를 반환합니다.

```
log2(numeric_expression)
```

**이름 \***

- **numeric 압축**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `nanvl`

첫 번째 인수를 반환하지 않는 경우*뚱 베어*·
두 번째 인수를 그렇지 않으면 반환합니다.

```
nanvl(expression_x, expression_y)
```

**이름 \***

- **파일 형식**: 숫자 표현이 아닌 경우 반환*뚱 베어*·
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.
- **본문내용**: 첫 번째 표현이 있는 경우 반환하는 숫자 표현*뚱 베어*·
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `pi`

π의 대략적인 값을 반환합니다.

```
pi()
```

## `power`

exponent의 힘으로 제기 된 기본 표현을 반환합니다.

```
power(base, exponent)
```

**이름 \***

- **기본 정보**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.
- **회사연혁**: 운영되는 노출 수치 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

**이름 \***

- 뚱 베어

## `pow`

*이름 \*[출력 전력](#power).*

## `radians`

변환 도 에 radians.

```
radians(numeric_expression)
```

**이름 \***

- **numeric 압축**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `random`

범위 \[0, 1)의 임의 float 값을 반환합니다.
무작위 씨앗은 각 행에 독특합니다.

```
random()
```

## `round`

가장 가까운 정수에 번호 라운드.

```
round(numeric_expression[, decimal_places])
```

**이름 \***

- **numeric 압축**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.
- **소수점**: 옵션. 소수의 장소의 숫자로 라운드합니다.
  기본값 0.

## `signum`

번호를 반환합니다.
부정 번호 반환`-1`·
Zero 및 긍정적인 숫자 반환`1`.

```
signum(numeric_expression)
```

**이름 \***

- **numeric 압축**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `sin`

숫자의 사인을 반환합니다.

```
sin(numeric_expression)
```

**이름 \***

- **numeric 압축**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `sinh`

숫자의 hyperbolic 사인을 반환합니다.

```
sinh(numeric_expression)
```

**이름 \***

- **numeric 압축**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `sqrt`

숫자의 사각형 뿌리를 반환합니다.

```
sqrt(numeric_expression)
```

**이름 \***

- **numeric 압축**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `tan`

숫자의 tangent를 반환합니다.

```
tan(numeric_expression)
```

**이름 \***

- **numeric 압축**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `tanh`

숫자의 hyperbolic tangent를 돌려줍니다.

```
tanh(numeric_expression)
```

**이름 \***

- **numeric 압축**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

## `trunc`

지정된 소수점 장소로 전체 번호 또는 truncated 수를 전달합니다.

```
trunc(numeric_expression[, decimal_places])
```

**이름 \***

- **numeric 압축**: 동작하는 숫자 표현.
  일정한, 란, 또는 기능 및 arithmetic 통신수의 어떤 조합일 수 있습니다.

- **소수점**: 옵션. 소수의 장소 수
  truncate에. 기본값은 0 (전체 번호에 따라). 이름 \*
  `decimal_places`긍정적 인 정수, truncates 자리
  소수점의 오른쪽. 이름 \*`decimal_places`부정 행위
  integer는 소수점의 왼쪽에 자리합니다.`0`.
