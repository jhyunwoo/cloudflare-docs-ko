---
summary: Build a WebSocket server using Durable Objects and Workers.
tags:
  - WebSockets
pcx_content_type: example
title: WebSocket 서버 구축
sidebar:
  order: 3
description: Durable Objects 및 WorkersXQ를 사용하여 WebSocket 서버 구축
reviewed: 2024-02-28T00:00:00.000Z
translation_locale: ko-KR
translation_source_html: 'https://developers.cloudflare.com/durable-objects/examples/websocket-server/'
translation_source_md: >-
  https://developers.cloudflare.com/durable-objects/examples/websocket-server/index.md
translation_source_kind: repo-mdx
translation_snapshot: '2026-03-13'
translation_upstream_ref: 66ee2dcd3715f1ca46e70954a3cda63f6cbebc75

---
import { TabItem, Tabs, GlossaryTooltip, WranglerConfig, TypeScriptExample } from "~/components";

이 예제는 WebSocket 서버 구축 방법을 보여줍니다.<GlossaryTooltip term="Durable Object">Durable Objects</GlossaryTooltip>그리고 Workers. 예제는 새로운 WebSocket 연결을 만드는 엔드포인트를 노출합니다. 이 WebSocket 연결은 현재 설치된 WebSocket 연결의 총 수를 포함하여 모든 메시지를 에코합니다. 더 많은 정보를 원하시면, 참조[WebSockets에 Durable Objects 사용](/durable-objects/best-practices/websockets/).

:::카트로션

WebSocket 연결은 튼튼한 개체를 메모리에 핀, 그래서 내구 요금은 WebSocket이 연결되는 것과 같이 너무 길어질 것입니다 (활동의 무시). inactivity의 기간 도중 내구 요금을 피하기 위하여는, 사용[웹소켓 Hibernation API](/durable-objects/examples/websocket-hibernation-server/)JavaScript가 적극적으로 실행될 때만 지속될 책임이 있는 경우에.

:::

<Tabs syncKey="workersExamples">
  <TypeScriptExample omitTabs={true}>
    ```ts
    import { DurableObject } from 'cloudflare:workers';

    //작업자
    export default {
        async fetch(request: Request, env: Env, ctx: ExecutionContext): Promise<Response> {
            if (request.url.endsWith('/websocket')) {
                //WebSocket 업그레이드 요청을 받기를 기대합니다.
                //하나 있다면, 요청을 받아 WebSocket 응답을 반환합니다.
                const upgradeHeader = request.headers.get('Upgrade');
                if (!upgradeHeader || upgradeHeader !== 'websocket') {
                    return new Response('Worker expected Upgrade: websocket', {
                        status: 426,
                    });
                }

                if (request.method !== 'GET') {
                    return new Response('Worker expected GET method', {
                        status: 400,
                    });
                }

                //우리는 연속 이름 'foo'를 제공함으로써 튼튼한 개체 ID를 코딩하는 것은 어렵습니다.
                //이 Worker의 모든 요청은 동일한 튼튼한 Object 인스턴스로 전송됩니다.
                let id = env.WEBSOCKET_SERVER.idFromName('foo');
                let stub = env.WEBSOCKET_SERVER.get(id);

                return stub.fetch(request);
            }

            return new Response(
                `Supported endpoints:
    /websocket: Expects a WebSocket upgrade request`,
                {
                    status: 200,
                    headers: {
                        'Content-Type': 'text/plain',
                    },
                }
            );
        },
    };

    //튼튼한 목표
    export class WebSocketServer extends DurableObject {
        //모든 WebSocket 연결 트랙 유지
        sessions: Map<WebSocket, { [key: string]: string }>;

        constructor(ctx: DurableObjectState, env: Env) {
            super(ctx, env);
            this.sessions = new Map();
        }

        async fetch(request: Request): Promise<Response> {
            //WebSocket 연결의 두 끝을 만듭니다.
            const webSocketPair = new WebSocketPair();
            const [client, server] = Object.values(webSocketPair);

            //`accept()`를 호출하면 이 WebSocket이 종료되기 시작합니다.
            //튼튼한 목표 내의 요청. 그것은 "accepting"의 효과,
            //그리고 WebSocket을 전송하고 메시지를 수신 할 수 있습니다.
            server.accept();

            //세션에 임의 UUID 생성.
            const id = crypto.randomUUID();
            //WebSocket 연결을 Active 세션 맵에 추가합니다.
            this.sessions.set(server, { id });

            server.addEventListener('message', (event) => {
                this.handleWebSocketMessage(server, event.data);
            });

            //클라이언트가 연결을 닫으면, runtime은 연결도 닫힙니다.
            server.addEventListener('close', () => {
                this.handleConnectionClose(server);
            });

            return new Response(null, {
                status: 101,
                webSocket: client,
            });
        }

        async handleWebSocketMessage(ws: WebSocket, message: string | ArrayBuffer) {
            const connection = this.sessions.get(ws)!;

            //응답과 같은 메시지와 연결
            ws.send(`[Durable Object] message: ${message}, from: ${connection.id}, to: the initiating client. Total connections: ${this.sessions.size}`);

            //모든 연결에 메시지를 방송,
            //메시지가 전송되는 것을 제외하고.
            this.sessions.forEach((_, session) => {
                if (session !== ws) {
                    session.send(`[Durable Object] message: ${message}, from: ${connection.id}, to: all clients except the initiating client. Total connections: ${this.sessions.size}`);
                }
            });

            //모든 연결에 메시지를 방송,
            //메시지가 전송되는 것을 포함하여.
            this.sessions.forEach((_, session) => {
                session.send(`[Durable Object] message: ${message}, from: ${connection.id}, to: all clients. Total connections: ${this.sessions.size}`);
            });
        }

        async handleConnectionClose(ws: WebSocket) {
            this.sessions.delete(ws);
            ws.close(1000, 'Durable Object is closing WebSocket');
        }
    }
    ```
  </TypeScriptExample>

  <TabItem label="모델 번호: Python" icon="seti:python">
    ```py
    from workers import DurableObject, Response, WorkerEntrypoint
    from js import WebSocketPair
    from pyodide.ffi import create_proxy
    import uuid

    class Session:
      def __init__(self, *, ws):
        self.ws = ws

    #작업자
    class Default(WorkerEntrypoint):
    	async def fetch(self, request):
    		if request.url.endswith('/websocket'):
    			#WebSocket 업그레이드 요청을 받기를 기대합니다.
    			#하나 있다면, 요청을 받아 WebSocket 응답을 반환합니다.
    			upgrade_header = request.headers.get('Upgrade')
    			if not upgrade_header or upgrade_header != 'websocket':
    				return Response('Worker expected Upgrade: websocket', status=426)

    			if request.method != 'GET':
    				return Response('Worker expected GET method', status=400)

    			#우리는 연속 이름 'foo'를 제공함으로써 튼튼한 개체 ID를 코딩하는 것은 어렵습니다.
    			#이 Worker의 모든 요청은 동일한 튼튼한 Object 인스턴스로 전송됩니다.
    			id = self.env.WEBSOCKET_SERVER.idFromName('foo')
    			stub = self.env.WEBSOCKET_SERVER.get(id)

    			return await stub.fetch(request)

    		return Response(
    			"""Supported endpoints:
    /websocket: Expects a WebSocket upgrade request""",
    			status=200,
    			headers={'Content-Type': 'text/plain'}
    		)

    #튼튼한 목표
    class WebSocketServer(DurableObject):
    	def __init__(self, ctx, env):
    		super().__init__(ctx, env)
    		#모든 WebSocket 연결의 추적을 유지, 세션 ID에 의해 키
    		self.sessions = {}

    	async def fetch(self, request):
    		#WebSocket 연결의 두 끝을 만듭니다.
    		client, server = WebSocketPair.new().object_values()

    		#`accept()`를 호출하면 이 WebSocket이 종료되기 시작합니다.
    		#튼튼한 목표 내의 요청. 그것은 "accepting"의 효과,
    		#그리고 WebSocket을 전송하고 메시지를 수신 할 수 있습니다.
    		server.accept()

    		#세션에 임의 UUID 생성.
    		id = str(uuid.uuid4())

    		#이벤트 핸들러에 대한 프록시 생성 (여기 소켓이 닫을 때 파괴됩니다)
    		async def on_message(event):
    			await self.handleWebSocketMessage(id, event.data)

    		message_proxy = create_proxy(on_message)
    		server.addEventListener('message', message_proxy)

    		#클라이언트가 연결을 닫으면, runtime은 연결도 닫힙니다.
    		async def on_close(event):
    			await self.handleConnectionClose(id)
    			#프록시 설정
    			message_proxy.destroy()
    			close_proxy.destroy()

    		close_proxy = create_proxy(on_close)
    		server.addEventListener('close', close_proxy)

    		#WebSocket 연결이 활성화된 세션의 맵에 추가, 세션 ID에 의해 키.
    		self.sessions[id] = Session(ws=server)

    		return Response(None, status=101, web_socket=client)

    	async def handleWebSocketMessage(self, session_id, message):
    		session = self.sessions[session_id]

    		#응답과 같은 메시지와 연결
    		session.ws.send(f"[Durable Object] message: {message}, from: {session_id}, to: the initiating client. Total connections: {len(self.sessions)}")

    		#모든 연결에 메시지를 방송,
    		#메시지가 전송되는 것을 제외하고.
    		for id, conn in self.sessions.items():
    			if id != session_id:
    				conn.ws.send(f"[Durable Object] message: {message}, from: {session_id}, to: all clients except the initiating client. Total connections: {len(self.sessions)}")

    		#모든 연결에 메시지를 방송,
    		#메시지가 전송되는 것을 포함하여.
    		for id, conn in self.sessions.items():
    			conn.ws.send(f"[Durable Object] message: {message}, from: {session_id}, to: all clients. Total connections: {len(self.sessions)}")

    	async def handleConnectionClose(self, session_id):
    		session = self.sessions.pop(session_id, None)
    		if session:
    			session.ws.close(1000, 'Durable Object is closing WebSocket')
    ```
  </TabItem>
</Tabs>

마지막으로, Wrangler 파일을 구성하여 튼튼한 개체를 포함[관련 제품](/durable-objects/get-started/#4-configure-durable-object-bindings)·[회사연혁](/durable-objects/reference/durable-objects-migrations/)에 따라<GlossaryTooltip term="namespace">이름공간</GlossaryTooltip>그리고 클래스 이름 이전에 선택.

<WranglerConfig>
  ```jsonc
  {
  	"$schema": "./node_modules/wrangler/config-schema.json",
  	"name": "websocket-server",
  	"main": "src/index.ts",
  	"durable_objects": {
  		"bindings": [
  			{
  				"name": "WEBSOCKET_SERVER",
  				"class_name": "WebSocketServer"
  			}
  		]
  	},
  	"migrations": [
  		{
  			"tag": "v1",
  			"new_sqlite_classes": [
  				"WebSocketServer"
  			]
  		}
  	]
  }
  ```
</WranglerConfig>

### 관련 자료

- [Durable Objects: 가장자리 채팅 데모](https://github.com/cloudflare/workers-chat-demo).
