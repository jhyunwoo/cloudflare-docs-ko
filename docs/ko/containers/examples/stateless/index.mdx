---
summary: Run multiple instances across Cloudflare's network
pcx_content_type: example
title: Stateless 인스턴스
sidebar:
  order: 2
description: Cloudflare의 네트워크를 통해 여러 인스턴스를 실행
reviewed: 2025-06-24T00:00:00.000Z
translation_locale: ko-KR
translation_source_html: 'https://developers.cloudflare.com/containers/examples/stateless/'
translation_source_md: 'https://developers.cloudflare.com/containers/examples/stateless/index.md'
translation_source_kind: repo-mdx
translation_snapshot: '2026-03-13'
translation_upstream_ref: 66ee2dcd3715f1ca46e70954a3cda63f6cbebc75

---
컨테이너의 여러 인스턴스 중 하나에 간단히 프록시 요청하려면 사용할 수 있습니다.`getRandom`기능:

```ts
import { Container, getRandom } from "@cloudflare/containers";

const INSTANCE_COUNT = 3;

class Backend extends Container {
	defaultPort = 8080;
	sleepAfter = "2h";
}

export default {
	async fetch(request: Request, env: Env): Promise<Response> {
		//참고 : "getRandom"은 가까운 미래에 지연으로 대체됩니다.
		const containerInstance = await getRandom(env.BACKEND, INSTANCE_COUNT);
		return containerInstance.fetch(request);
	},
};
```

:::참조
이 예제는`getRandom`함수는, 랜덤하게 될 임시 돕는
요청을 경로로 컨테이너의 N 인스턴스 중 하나를 선택합니다.

앞으로, 우리는 개량한 latency-aware 짐 균형을 잡고 Autoscaling를 제공할 것입니다.

이것은 stateless 인스턴스를 간단하고 더 효율적으로 routing 할 것입니다. 이름 \*
[자동화 문서](/containers/platform-details/scaling-and-routing)더 많은 정보.
주요 특징
