---
pcx_content_type: concept
title: 임의화
sidebar:
  order: 2
translation_locale: ko-KR
translation_source_html: >-
  https://developers.cloudflare.com/randomness-beacon/cryptographic-background/randomness-generation/
translation_source_md: >-
  https://developers.cloudflare.com/randomness-beacon/cryptographic-background/randomness-generation/index.md
translation_source_kind: repo-mdx
translation_snapshot: '2026-03-13'
translation_upstream_ref: 66ee2dcd3715f1ca46e70954a3cda63f6cbebc75

---
이 섹션에서는, 우리는이 공동 키 쌍을 사용하는 방법을 설명합니다. 공개적으로 검증 가능한, unbiasable, 분산 된 방식으로 불확실한 무작위.

첫째, 우리는 매우 대중적 인 쌍 기반 암호화 (PBC)를 설명하고 zk-SNARKs와 같은 많은 현대 합의 프로토콜 또는 Zero-knowledge 증거에서 사용됩니다. 우리는 그 후 임계의 비콘 생성 단계에 대한 drand가 PBC를 사용하는 방법을 보여줍니다. Boneh-Lynn-Shacham (BLS) 서명. 마지막으로, 생성 된 임계값 BLS 서명을 랜덤 체인으로 연결하는 방법을 논의합니다.

## 쌍 기반 암호화

쌍 기반 암호화는 bilinear 그룹을 기반으로합니다.`(𝔾1,𝔾2,𝔾𝑡)`, 어디`𝔾1`, `𝔾2`·`𝔾𝑡`주요한 순서의 주기적인 그룹입니다`𝑝`발전기`𝑔1`, `𝑔2`·`𝑔𝑡`, 각각 및 쌍 가동`𝑒:𝔾1×𝔾2→𝔾𝑡`이 재산으로:

- **비선형:** `∀𝑎,𝑏∈ℤ∗𝑝,∀𝑃∈𝔾1,∀𝑄∈𝔾2,`우리는 있습니다`𝑒(𝑎𝑃,𝑏𝑄)=𝑒(𝑃,𝑄)𝑎𝑏`

- **비 degeneracy:** `𝑒≠1`

- **공급 능력:**&#xD6A8;율적인 알고리즘이 존재합니다.`𝑒`·
  drand는 현재 Barreto-Lynn-Scott 곡선 BLS12-381을 사용합니다.

## BLS 서명

공개적으로 검증할 수 있도록, unbiasable, 분산 랜덤, 드론은 임계값 Boneh-Lynn-Shacham (BLS) 서명을 사용합니다. 첫번째 우리는 정규적인 BLS 서명 및 그 후에 문턱을 설명할 것입니다.

BLS 서명은 bilinear 페어링에 의존하는 짧은 서명이며 단일 요소 만 이루어져 있습니다.`𝔾1`. 그들은 ECDSA와 같은 다른 서명 방식과는 달리 메시지와 서명자의 키에만 의존하는 감각에 대한 신중한 값이 필요하며, 각 서명 된 메시지에 대한 신선한 임의 값을 확보해야합니다. 동일한 키로 생성 된 주어진 메시지에 다르게 두 개의 BLS 서명이 동일합니다. drand에서, 우리는 임의 세대에 대한 unbiasability를 달성하기 위해이 특성을 사용합니다.

BLS 서명 방식은 이러한 하위 절차로 구성됩니다.

### 키 생성

열쇠 쌍을 생성하기 위하여, signer는 첫번째 개인 열쇠를 선택합니다,`𝑥∈ℤ∗𝑝`, 무작위로, 그리고 그 후에 대응 공중 열쇠를 따릅니다`𝑋=𝑔𝑥2∈𝔾2`.

### 회사연혁

다운로드`𝐻:{0,1}∗→𝔾1`denote a cryptographic hash function that maps arbitrary bit strings 에 요소의`𝔾1`. BLS 서명을 준수하기`𝜎`이름 \*`𝑚`, signer 보상`𝜎=𝑥𝐻(𝑚)∈𝔾1`.

### 서명 검증

BLS 서명 확인`𝜎`이름 \*`𝑚`유효하다, verifier checks if`𝑒(𝐻(𝑚),𝑋)=𝑒(𝜎,𝑔2)`signer의 공개 키 사용`𝑋`.

이 방정식은 유효 시그널을 보유합니다.`𝑒(𝐻(𝑚),𝑋)=𝑒(𝐻(𝑚),𝑔𝑥2)=𝑒(𝐻(𝑚),𝑔2)𝑥=𝑒(𝑥𝐻(𝑚),𝑔2)=𝑒(𝜎,𝑔2)`.

## Threshold BLS 서명

임계 값 서명 계획의 목표는 참가자가 독립적으로 생성 한 개별 부분 서명을 결합하여 서명을 구성하는 것입니다. 임계값 BLS 서명 방식은 다음과 같은 하위 절차가 있습니다.

### 키 생성

더 보기`𝑛`참가자들은`𝑡-of-𝑛`DKG는 공동 공개 키 설정,`𝑆∈𝔾2`, 개인 키 공유`𝑠𝑖∈ℤ∗𝑝`알 수없는 공동 개인 키의,`𝑠`위와 같이.

### Partial 서명 발생

메시지를 표시하려면`𝑚`, 각`𝑖`개인 키 공유를 사용합니다.`𝑠𝑖`, 부분 BLS 서명을 만들려면,`𝜎𝑖=𝑠𝑖𝐻(𝑚)`.

### Partial 서명 검증

부분 서명의 정정을 확인하려면`𝜎𝑖`, 에`𝑚`, verifier는 공공 키 공유를 사용합니다,`𝑆𝑖`, DKG 도중 생성해, 그(것)들을 검증합니다`𝑒(𝐻(𝑚),𝑆𝑖)=𝑒(𝜎𝑖,𝑔2)`회사연혁

### 서명 재건

BLS 서명을 재구성하려면`𝜎`에 의해`𝑚`, verifier 첫번째 수집`𝑡`다른 유효한 부분 BLS 서명,`𝜎𝑖`, 에`𝑚`Lagrange interpolation에 의해 따르십시오.

### 서명 검증

BLS 서명을 확인하려면`𝜎`, verifier 체크 그`𝑒(𝐻(𝑚),𝑆)=𝑒(𝜎,𝑔2)`현재 위치`𝑆`공용 키입니다.

Lagrange interpolation의 속성 덕분에 가치`𝜎`하위 세트의 독립적입니다`𝑡`유효한 부분 서명,`𝜎𝑖`시그니처 재건축 중 선택 또한, Lagrange interpolation는 또한 더 적은의 세트를 보장합니다`𝑡`signers는 예측할 수 있습니다 또는 bias`𝜎`.

요약, 임계값 BLS 서명,`𝜎`, 공개적으로 verifiable, unbiasable, unpredictable 및 분배된 무작위를 위해 요구되는 모든 재산을 전시하십시오.

### `𝔾1/𝔾2`관련 링크

위에서,`𝔾1`·`𝔾2`교환 될 수 있습니다. implication은 대중적인 열쇠 및 서명의 상대적인 크기에 있습니다. 첫 번째 드래곤 체인은 위에 설명되어 있으며, 서명과 함께`𝔾2`그리고 공공 키에`𝔾1`. 서명 크기는 96 바이트이며, 공공 키 크기는 48 바이트입니다.

특정 응용 프로그램은 더 큰 공공 키의 비용에 더 작은 서명을 선호합니다. 왜 특정 drand beacons에는 서명이 있습니다.`𝔾1`그리고 공공 키에`𝔾2`. 그런 변화는 것과 같이 불립니다`𝔾1/𝔾2 swap`.

## 연락처

드래곤 랜섬 비콘은 분리된 라운드에서 작동,`𝑟`. 모든 라운드에서, 랜딩 랜딩 임의성을 사용하여 새로운 임의 값을 임계값을 생성하기 위해 구성된 드래곤 비콘은 임계의 사슬로 연결됩니다. 무작위의 이 사슬을 확장하기 위하여, 각 drand participant,`𝑖`, 둥근에서 창조하십시오`𝑟`부분 BLS 서명,`𝜎𝑟𝑖`메시지에`𝑚=𝐻(𝑟∥𝜎𝑟−1)`현재 위치`𝜎𝑟−1`(전체) BLS 임계값 서명을 라운드에서 나타냅니다.`𝑟−1`·`𝐻`암호화 해시 함수.

더 이상`𝑡`참가자는 부분 서명을 방송했습니다.`𝜎𝑟𝑖`, 에`𝑚`, 누구든지 가득 차있는 BLS 문턱 서명을 복구할 수 있습니다,`𝜎𝑟`그것은 둥근의 무작위 값에 대응`𝑟`. 이 후, drand 노드가 라운드로 이동`𝑟+1`그리고 프로세스를 reiterate.

둥근`𝑟=0`, 드래곤 참가자는 드래곤 설정 중 씨앗을 고정합니다. 이 프로세스는 모든 새로운 임의 값이 이전 생성 된 서명에 따라 달라집니다. 서명이 결정되기 때문에, 체인을 포용하고 두 개의 명백한 서명을 제시하는 adversary에 대한 가능성이 없습니다.`𝜎𝑟`·`𝜎′𝑟`주어진 라운드에서`𝑟`공공 임의에 의존하는 시스템의 inconsistencies를 생성합니다.

## Unchained 랜덤

drand beacons도 unchained 랜덤을 사용할 수 있습니다. 무작위의 이 사슬을 확장하기 위하여, 각 drand participant,`𝑖`, 둥근에서 창조하십시오`𝑟`부분 BLS 서명,`𝜎𝑟𝑖`메시지에`𝑚=𝐻(𝑟)`이름 \*`𝐻`암호화 해시 함수.

이 과정은 메시지의 직접적인 precomputation를 허용합니다`𝑚`관련 제품`𝑟=i`.
