---
pcx_content_type: tutorial
title: Interactive ChatGPT 앱 구축
sidebar:
  order: 6
reviewed: 2025-11-07T00:00:00.000Z
translation_locale: ko-KR
translation_source_html: 'https://developers.cloudflare.com/agents/guides/chatgpt-app/'
translation_source_md: 'https://developers.cloudflare.com/agents/guides/chatgpt-app/index.md'
translation_source_kind: repo-mdx
translation_snapshot: '2026-03-13'
translation_upstream_ref: 66ee2dcd3715f1ca46e70954a3cda63f6cbebc75
---
import {
	Details,
	Render,
	PackageManagers,
	WranglerConfig,
	LinkCard,
} from "~/components";

## 첫 번째 ChatGPT 앱 배포

이 가이드는 CloudflareWorkers에서 대화형 ChatGPT 앱을 구축하고 배포하는 방법을 보여줍니다.

- ChatGPT 대화에서 직접 풍부한 대화 형 UI 위젯 렌더링
- Durable Objects를 사용하여 실시간 멀티 유저 상태를 유지
- 앱과 ChatGPT 간의 양방향 통신 가능
- ChatGPT 내에서 실행되는 멀티플레이어 경험 구축

이 기능을 설명하는 실시간 멀티 플레이어 체스 게임을 구축 할 것입니다. 플레이어는 게임을 시작하거나 가입 할 수 있으며 대화를 떠나지 않고 전략적인 조언을위한 ChatGPT를 요청합니다.

채팅GPT 앱 사용**모형 Context 의정서 (MCP)**&#x43;hatGPT의 도구와 UI 리소스를 대리할 수 있습니다.

이 예제의 전체 코드를 볼 수 있습니다.[이름 \*](https://github.com/cloudflare/agents/tree/main/openai-sdk/chess-app).

## 자주 묻는 질문

시작하기 전에 다음을해야합니다.

- ·[Cloudflare 계정](https://dash.cloudflare.com/sign-up)
- [Node.js\`\`\`\`\`\`\`](https://nodejs.org/)설치 (v18 이상)
- ·[ChatGPT Plus 또는 팀 계정](https://chat.openai.com/)개발자 모드 활성화
- React와 TypeScript의 기본 지식

## 1. Enable ChatGPT 개발자 형태

ChatGPT Apps를 사용하려면 개발자 모드를 활성화해야합니다.

1. 기타[채팅GPT](https://chat.openai.com/).
2. 오시는 길**계정 설정** > **앱 및 커넥터** > **고급 설정**
3. 제품정보**개발자 모드 ON**

일단 활성화되면 개발 및 테스트 중에 사용자 정의 앱을 설치할 수 있습니다.

## 2. ChatGPT 앱 프로젝트 만들기

1. Chess App에 대한 새로운 프로젝트 만들기:

<PackageManagers type="create" pkg="cloudflare@latest" args={"my-chess-app"} />

2. 프로젝트에 Navigate:

```sh
cd my-chess-app
```

3. 필요한 의존도를 설치하십시오:

```sh
npm install agents @modelcontextprotocol/sdk chess.js react react-dom react-chessboard
```

4. 개발 의존성 설치:

```sh
npm install -D @cloudflare/vite-plugin @vitejs/plugin-react vite vite-plugin-singlefile @types/react @types/react-dom
```

## 3. 프로젝트 구성

1. 업데이트`wrangler.jsonc`Durable Objects와 자산을 구성하려면:

<WranglerConfig>
  ```jsonc
  {
  	"name": "my-chess-app",
  	"main": "src/index.ts",
  	"compatibility_date": "$today",
  	"compatibility_flags": ["nodejs_compat"],
  	"durable_objects": {
  		"bindings": [
  			{
  				"name": "CHESS",
  				"class_name": "ChessGame",
  			},
  		],
  	},
  	"migrations": [
  		{
  			"tag": "v1",
  			"new_sqlite_classes": ["ChessGame"],
  		},
  	],
  	"assets": {
  		"directory": "dist",
  		"binding": "ASSETS",
  	},
  }
  ```
</WranglerConfig>

2. 이름 \*`vite.config.ts`React UI를 구축하려면:

```ts
import { cloudflare } from "@cloudflare/vite-plugin";
import react from "@vitejs/plugin-react";
import { defineConfig } from "vite";
import { viteSingleFile } from "vite-plugin-singlefile";

export default defineConfig({
	plugins: [react(), cloudflare(), viteSingleFile()],
	build: {
		minify: false,
	},
});
```

3. 업데이트`package.json`스크립트:

```json
{
	"scripts": {
		"dev": "vite",
		"build": "vite build",
		"deploy": "vite build && wrangler deploy"
	}
}
```

## 4. 체스 게임 엔진 만들기

1. Durable Objects를 사용하여 게임 논리 만들기`src/chess.tsx`:

```tsx
import { Agent, callable, getCurrentAgent } from "agents";
import { Chess } from "chess.js";

type Color = "w" | "b";

type ConnectionState = {
	playerId: string;
};

export type State = {
	board: string;
	players: { w?: string; b?: string };
	status: "waiting" | "active" | "mate" | "draw" | "resigned";
	winner?: Color;
	lastSan?: string;
};

export class ChessGame extends Agent<Env, State> {
	initialState: State = {
		board: new Chess().fen(),
		players: {},
		status: "waiting",
	};

	game = new Chess();

	constructor(
		ctx: DurableObjectState,
		public env: Env,
	) {
		super(ctx, env);
		this.game.load(this.state.board);
	}

	private colorOf(playerId: string): Color | undefined {
		const { players } = this.state;
		if (players.w === playerId) return "w";
		if (players.b === playerId) return "b";
		return undefined;
	}

	@callable()
	join(params: { playerId: string; preferred?: Color | "any" }) {
		const { playerId, preferred = "any" } = params;
		const { connection } = getCurrentAgent();
		if (!connection) throw new Error("Not connected");

		connection.setState({ playerId });
		const s = this.state;

		//이미 좌석? 반환 좌석
		const already = this.colorOf(playerId);
		if (already) {
			return { ok: true, role: already as Color, state: s };
		}

		//좌석 선택
		const free: Color[] = (["w", "b"] as const).filter((c) => !s.players[c]);
		if (free.length === 0) {
			return { ok: true, role: "spectator" as const, state: s };
		}

		let seat: Color = free[0];
		if (preferred === "w" && free.includes("w")) seat = "w";
		if (preferred === "b" && free.includes("b")) seat = "b";

		s.players[seat] = playerId;
		s.status = s.players.w && s.players.b ? "active" : "waiting";
		this.setState(s);
		return { ok: true, role: seat, state: s };
	}

	@callable()
	move(
		move: { from: string; to: string; promotion?: string },
		expectedFen?: string,
	) {
		if (this.state.status === "waiting") {
			return {
				ok: false,
				reason: "not-in-game",
				fen: this.game.fen(),
				status: this.state.status,
			};
		}

		const { connection } = getCurrentAgent();
		if (!connection) throw new Error("Not connected");
		const { playerId } = connection.state as ConnectionState;

		const seat = this.colorOf(playerId);
		if (!seat) {
			return {
				ok: false,
				reason: "not-in-game",
				fen: this.game.fen(),
				status: this.state.status,
			};
		}

		if (seat !== this.game.turn()) {
			return {
				ok: false,
				reason: "not-your-turn",
				fen: this.game.fen(),
				status: this.state.status,
			};
		}

		//Optimistic sync 감시
		if (expectedFen && expectedFen !== this.game.fen()) {
			return {
				ok: false,
				reason: "stale",
				fen: this.game.fen(),
				status: this.state.status,
			};
		}

		const res = this.game.move(move);
		if (!res) {
			return {
				ok: false,
				reason: "illegal",
				fen: this.game.fen(),
				status: this.state.status,
			};
		}

		const fen = this.game.fen();
		let status: State["status"] = "active";
		if (this.game.isCheckmate()) status = "mate";
		else if (this.game.isDraw()) status = "draw";

		this.setState({
			...this.state,
			board: fen,
			lastSan: res.san,
			status,
			winner:
				status === "mate" ? (this.game.turn() === "w" ? "b" : "w") : undefined,
		});

		return { ok: true, fen, san: res.san, status };
	}

	@callable()
	resign() {
		const { connection } = getCurrentAgent();
		if (!connection) throw new Error("Not connected");
		const { playerId } = connection.state as ConnectionState;

		const seat = this.colorOf(playerId);
		if (!seat) return { ok: false, reason: "not-in-game", state: this.state };

		const winner = seat === "w" ? "b" : "w";
		this.setState({ ...this.state, status: "resigned", winner });
		return { ok: true, state: this.state };
	}
}
```

## 5. MCP 서버 및 UI 리소스 생성

1. 당신의 주요 노동자를 만들기`src/index.ts`:

```ts
import { createMcpHandler } from "agents/mcp";
import { routeAgentRequest } from "agents";
import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import { env } from "cloudflare:workers";

const getWidgetHtml = async (host: string) => {
	let html = await (await env.ASSETS.fetch("http://localhost/")).text();
	html = html.replace(
		"<!--RUNTIME_CONFIG-->",
		`<script>window.HOST = \`${host}\`;</script>`,
	);
	return html;
};

function createServer() {
	const server = new McpServer({ name: "Chess", version: "v1.0.0" });

	//ChatGPT가 렌더링 할 수있는 UI 리소스를 등록
	server.registerResource(
		"chess",
		"ui://widget/index.html",
		{},
		async (_uri, extra) => {
			return {
				contents: [
					{
						uri: "ui://widget/index.html",
						mimeType: "text/html+skybridge",
						text: await getWidgetHtml(
							extra.requestInfo?.headers.host as string,
						),
					},
				],
			};
		},
	);

	//ChatGPT가 UI를 렌더링할 수 있는 도구 등록
	server.registerTool(
		"playChess",
		{
			title: "Renders a chess game menu, ready to start or join a game.",
			annotations: { readOnlyHint: true },
			_meta: {
				"openai/outputTemplate": "ui://widget/index.html",
				"openai/toolInvocation/invoking": "Opening chess widget",
				"openai/toolInvocation/invoked": "Chess widget opened",
			},
		},
		async (_, _extra) => {
			return {
				content: [
					{ type: "text", text: "Successfully rendered chess game menu" },
				],
			};
		},
	);

	return server;
}

export default {
	async fetch(req: Request, env: Env, ctx: ExecutionContext) {
		const url = new URL(req.url);
		if (url.pathname.startsWith("/mcp")) {
			//요청 당 새로운 서버 인스턴스 만들기
			const server = createServer();
			return createMcpHandler(server)(req, env, ctx);
		}

		return (
			(await routeAgentRequest(req, env)) ??
			new Response("Not found", { status: 404 })
		);
	},
} satisfies ExportedHandler<Env>;

export { ChessGame } from "./chess";
```

## 6. React UI 구축

1. HTML 엔트리 포인트 만들기`index.html`:

```html
<!doctype html>
<html>
	<head>
		<!--런타임 CONFIG-->
	</head>
	<body>
		<div id="root" style="font-family: verdana"></div>
		<script type="module" src="/src/app.tsx"></script>
	</body>
</html>
```

2. React 앱 만들기`src/app.tsx`:

```tsx
import { useEffect, useRef, useState } from "react";
import { useAgent } from "agents/react";
import { createRoot } from "react-dom/client";
import { Chess, type Square } from "chess.js";
import { Chessboard, type PieceDropHandlerArgs } from "react-chessboard";
import type { State as ServerState } from "./chess";

function usePlayerId() {
	const [pid] = useState(() => {
		const existing = localStorage.getItem("playerId");
		if (existing) return existing;
		const id = crypto.randomUUID();
		localStorage.setItem("playerId", id);
		return id;
	});
	return pid;
}

function App() {
	const playerId = usePlayerId();
	const [gameId, setGameId] = useState<string | null>(null);
	const [gameIdInput, setGameIdInput] = useState("");
	const [menuError, setMenuError] = useState<string | null>(null);

	const gameRef = useRef(new Chess());
	const [fen, setFen] = useState(gameRef.current.fen());
	const [myColor, setMyColor] = useState<"w" | "b" | "spectator">("spectator");
	const [pending, setPending] = useState(false);
	const [serverState, setServerState] = useState<ServerState | null>(null);
	const [joined, setJoined] = useState(false);

	const host = window.HOST ?? "http://localhost:5173/";

	const { stub } = useAgent<ServerState>({
		host,
		name: gameId ?? "__lobby__",
		agent: "chess",
		onStateUpdate: (s) => {
			if (!gameId) return;
			gameRef.current.load(s.board);
			setFen(s.board);
			setServerState(s);
		},
	});

	useEffect(() => {
		if (!gameId || joined) return;

		(async () => {
			try {
				const res = await stub.join({ playerId, preferred: "any" });
				if (!res?.ok) return;

				setMyColor(res.role);
				gameRef.current.load(res.state.board);
				setFen(res.state.board);
				setServerState(res.state);
				setJoined(true);
			} catch (error) {
				console.error("Failed to join game", error);
			}
		})();
	}, [playerId, gameId, stub, joined]);

	async function handleStartNewGame() {
		const newId = crypto.randomUUID();
		setGameId(newId);
		setGameIdInput(newId);
		setMenuError(null);
		setJoined(false);
	}

	async function handleJoinGame() {
		const trimmed = gameIdInput.trim();
		if (!trimmed) {
			setMenuError("Enter a game ID to join.");
			return;
		}
		setGameId(trimmed);
		setMenuError(null);
		setJoined(false);
	}

	const handleHelpClick = () => {
		window.openai?.sendFollowUpMessage?.({
			prompt: `Help me with my chess game. I am playing as ${myColor} and the board is: ${fen}. Please only offer written advice.`,
		});
	};

	function onPieceDrop({ sourceSquare, targetSquare }: PieceDropHandlerArgs) {
		if (!gameId || !sourceSquare || !targetSquare || pending) return false;

		const game = gameRef.current;
		if (myColor === "spectator" || game.turn() !== myColor) return false;

		const piece = game.get(sourceSquare as Square);
		if (!piece || piece.color !== myColor) return false;

		const prevFen = game.fen();

		try {
			const local = game.move({
				from: sourceSquare,
				to: targetSquare,
				promotion: "q",
			});
			if (!local) return false;
		} catch {
			return false;
		}

		const nextFen = game.fen();
		setFen(nextFen);
		setPending(true);

		stub
			.move({ from: sourceSquare, to: targetSquare, promotion: "q" }, prevFen)
			.then((r) => {
				if (!r.ok) {
					game.load(r.fen);
					setFen(r.fen);
				}
			})
			.finally(() => setPending(false));

		return true;
	}

	return (
		<div style={{ padding: "20px", background: "#f8fafc", minHeight: "100vh" }}>
			{!gameId ? (
				<div
					style={{
						maxWidth: "420px",
						margin: "0 auto",
						background: "#fff",
						borderRadius: "16px",
						padding: "24px",
					}}
				>
					<h1>Ready to play?</h1>
					<p>Start a new match or join an existing game.</p>
					<button
						onClick={handleStartNewGame}
						style={{
							padding: "12px",
							background: "#2563eb",
							color: "#fff",
							border: "none",
							borderRadius: "8px",
							cursor: "pointer",
							width: "100%",
						}}
					>
						Start a new game
					</button>
					<div style={{ marginTop: "16px" }}>
						<input
							placeholder="Paste a game ID"
							value={gameIdInput}
							onChange={(e) => setGameIdInput(e.target.value)}
							style={{
								width: "100%",
								padding: "10px",
								borderRadius: "8px",
								border: "1px solid #ccc",
							}}
						/>
						<button
							onClick={handleJoinGame}
							style={{
								marginTop: "8px",
								padding: "10px",
								background: "#0f172a",
								color: "#fff",
								border: "none",
								borderRadius: "8px",
								cursor: "pointer",
								width: "100%",
							}}
						>
							Join
						</button>
						{menuError && (
							<p style={{ color: "red", fontSize: "0.85rem" }}>{menuError}</p>
						)}
					</div>
				</div>
			) : (
				<div style={{ maxWidth: "600px", margin: "0 auto" }}>
					<div
						style={{
							background: "#fff",
							padding: "16px",
							borderRadius: "16px",
							marginBottom: "16px",
						}}
					>
						<h2>Game {gameId}</h2>
						<p>Status: {serverState?.status}</p>
						<button
							onClick={handleHelpClick}
							style={{
								padding: "10px",
								background: "#2563eb",
								color: "#fff",
								border: "none",
								borderRadius: "8px",
								cursor: "pointer",
							}}
						>
							Ask for help
						</button>
					</div>
					<div
						style={{
							background: "#fff",
							padding: "16px",
							borderRadius: "16px",
						}}
					>
						<Chessboard
							position={fen}
							onPieceDrop={onPieceDrop}
							boardOrientation={myColor === "b" ? "black" : "white"}
						/>
					</div>
				</div>
			)}
		</div>
	);
}

const root = createRoot(document.getElementById("root")!);
root.render(<App />);
```

:::참조
UI의 단순화된 버전입니다. 플레이어 슬롯, 더 나은 스타일링 및 게임 상태 관리와 완벽한 구현을 위해, 체크 아웃[GitHub의 전체 예](https://github.com/cloudflare/agents/tree/main/openai-sdk/chess-app/src/app.tsx).
:::

## 7. 빌드 및 배포

1. React UI를 빌드:

```sh
npm run build
```

'React\` 앱을 단일 HTML 파일로 컴파일`dist`디렉토리.

2. Cloudflare에 배포:

```sh
npx wrangler deploy
```

배포 후 앱 URL을 볼 수 있습니다:

```
https://my-chess-app.YOUR_SUBDOMAIN.workers.dev
```

## 8. ChatGPT에 연결

이제 ChatGPT에 배포된 앱을 연결합니다:

1. 기타[채팅GPT](https://chat.openai.com/).
2. 오시는 길**계정 설정** > **앱 및 커넥터** > **이름 \***
3. 앱 만들기**이름 \***, 그리고 선택적으로**이름 \***·**이름 \***.
4. MCP 엔드포인트를 입력하십시오:`https://my-chess-app.YOUR_SUBDOMAIN.workers.dev/mcp`.
5. 제품정&#xBCF4;**"인증 없음"**.
6. 제품정&#xBCF4;**"요금"**.

## 9. ChatGPT에서 체스를 재생

그것을 밖으로 시도:

1. ChatGPT 대화에서, 유형: "Let's play chess".
2. ChatGPT가 호출됩니다.`playChess`도구와 대화 형 체스 위젯 렌더링.
3. 제품정&#xBCF4;**"새로운 게임을 시작"**&#xAC8C;임 만들기.
4. 자신의 ChatGPT 대화를 통해 가입 할 수있는 친구와 게임 ID를 공유하십시오.
5. 보드에 조각을 드래그하여 이동합니다.
6. 제품정&#xBCF4;**"문제"**&#x43;hatGPT에서 전략적인 조언을 얻기

:::참조
당신은 수동으로 그것을 사용하는 첫번째로 신속한 상자에 있는 연결관을 선정할 필요가 있을지도 모릅니다. 제품정&#xBCF4;**"+"** > **"더 많은"** > **\[앱 이름]**.
:::

## 주요 개념

### MCP 서버

Model Context Protocol (MCP) 서버는 ChatGPT가 접근할 수 있는 도구와 리소스를 정의합니다. cross-client 응답 누설을 방지하기 위해 요청당 새로운 서버 인스턴스를 생성하는 주:

```ts
function createServer() {
	const server = new McpServer({ name: "Chess", version: "v1.0.0" });

	//ChatGPT가 렌더링 할 수있는 UI 리소스를 등록
	server.registerResource(
		"chess",
		"ui://widget/index.html",
		{},
		async (_uri, extra) => {
			return {
				contents: [
					{
						uri: "ui://widget/index.html",
						mimeType: "text/html+skybridge",
						text: await getWidgetHtml(
							extra.requestInfo?.headers.host as string,
						),
					},
				],
			};
		},
	);

	//ChatGPT가 UI를 렌더링할 수 있는 도구 등록
	server.registerTool(
		"playChess",
		{
			title: "Renders a chess game menu, ready to start or join a game.",
			annotations: { readOnlyHint: true },
			_meta: {
				"openai/outputTemplate": "ui://widget/index.html",
				"openai/toolInvocation/invoking": "Opening chess widget",
				"openai/toolInvocation/invoked": "Chess widget opened",
			},
		},
		async (_, _extra) => {
			return {
				content: [
					{ type: "text", text: "Successfully rendered chess game menu" },
				],
			};
		},
	);

	return server;
}
```

### 에이전트와 게임 엔진

더 보기`ChessGame`클래스 확장`Agent`stateful 게임 엔진을 만들려면 :

```tsx
export class ChessGame extends Agent<Env, State> {
  initialState: State = {
    board: new Chess().fen(),
    players: {},
    status: "waiting"
  };

  game = new Chess();

  constructor(
    ctx: DurableObjectState,
    public env: Env
  ) {
    super(ctx, env);
    this.game.load(this.state.board);
  }
```

각 게임은 자체 에이전트 인스턴스를 가져옵니다.

- **고립된 국가**게임 당
- **실시간 동기화**플레이어
- **주변관광**그 생존 노동자 재시작

### 자주 묻는 질문

사용 방법`@callable()`클라이언트가 invoke 할 수있는 방법을 노출하는 decorator :

```ts
@callable()
join(params: { playerId: string; preferred?: Color | "any" }) {
  const { playerId, preferred = "any" } = params;
  const { connection } = getCurrentAgent();
  if (!connection) throw new Error("Not connected");

  connection.setState({ playerId });
  const s = this.state;

  //이미 좌석? 반환 좌석
  const already = this.colorOf(playerId);
  if (already) {
    return { ok: true, role: already as Color, state: s };
  }

  //좌석 선택
  const free: Color[] = (["w", "b"] as const).filter((c) => !s.players[c]);
  if (free.length === 0) {
    return { ok: true, role: "spectator" as const, state: s };
  }

  let seat: Color = free[0];
  if (preferred === "w" && free.includes("w")) seat = "w";
  if (preferred === "b" && free.includes("b")) seat = "b";

  s.players[seat] = playerId;
  s.status = s.players.w && s.players.b ? "active" : "waiting";
  this.setState(s);
  return { ok: true, role: seat, state: s };
}
```

### React 통합

더 보기`useAgent`Hook은 React 앱을 튼튼한 개체에 연결합니다.

```tsx
const { stub } = useAgent<ServerState>({
	host,
	name: gameId ?? "__lobby__",
	agent: "chess",
	onStateUpdate: (s) => {
		gameRef.current.load(s.board);
		setFen(s.board);
		setServerState(s);
	},
});
```

에이전트의 통화 방법:

```tsx
const res = await stub.join({ playerId, preferred: "any" });
await stub.move({ from: "e2", to: "e4" });
```

### 양방향 통신

앱은 ChatGPT로 메시지를 보낼 수 있습니다:

```ts
const handleHelpClick = () => {
	window.openai?.sendFollowUpMessage?.({
		prompt: `Help me with my chess game. I am playing as ${myColor} and the board is: ${fen}. Please only offer written advice as there are no tools for you to use.`,
	});
};
```

이것은 현재 게임 상태에 대한 context와 ChatGPT 대화의 새로운 메시지를 만듭니다.

## 다음 단계

이제 작업 ChatGPT 앱을 가지고 있습니다.

- 추가 도구: MCP 도구 및 리소스를 통해 추가 기능과 UI를 노출.
- UI를 강화하십시오: React를 가진 더 정교한 공용영역을 건설하십시오.

## 관련 자료

<LinkCard title="에이전트 API" href="/agents/api-reference/agents-api/" description="Agents SDK를 위한 API 참고 자료를 완료합니다." />

<LinkCard title="Durable 개체" href="/durable-objects/" description="기본 인프라에 대해 알아보기." />

<LinkCard title="모형 Context 의정서" href="https://modelcontextprotocol.io/" description="MCP 사양 및 문서." />

<LinkCard title="OpenAI 앱 SDK" href="https://developers.openai.com/apps-sdk/" description="공식 OpenAI 앱 SDK 참고 자료" />
