---
title: 채팅 에이전트 만들기
pcx_content_type: tutorial
sidebar:
  order: 4
head: []
description: API 키 없이 Workers AI를 사용해 도구를 갖춘 스트리밍 AI 채팅 agent를 빌드하세요.
---

import { TypeScriptExample, WranglerConfig, LinkCard } from "~/components";

AI 응답을 스트리밍하고, 서버 측 도구를 호출하고, 브라우저에서 클라이언트 측 도구를 실행하고, 민감한 동작 전에는 사용자 승인을 요청하는 chat agent를 만들어 봅시다.

**무엇을 만들게 되나요:** Workers AI 기반 채팅 agent. 자동 실행 도구, 클라이언트 측 도구, 승인 필요 도구의 세 가지 tool type을 포함합니다.

**시간:** 약 15분

**사전 준비:**

- Node.js 18+
- Cloudflare account(무료 요금제로 가능)

## 1. 프로젝트 만들기

```sh
npm create cloudflare@latest chat-agent
```

프롬프트가 나오면 **"Hello World" Worker**를 선택하세요. 그런 다음 의존성을 설치합니다.

```sh
cd chat-agent
npm install agents @cloudflare/ai-chat ai workers-ai-provider zod
```

## 2. Wrangler 구성

`wrangler.jsonc`를 다음으로 교체합니다.

<WranglerConfig>

```jsonc
{
	"name": "chat-agent",
	"main": "src/server.ts",
	"compatibility_date": "$today",
	"compatibility_flags": ["nodejs_compat"],
	"ai": { "binding": "AI" },
	"durable_objects": {
		"bindings": [{ "name": "ChatAgent", "class_name": "ChatAgent" }],
	},
	"migrations": [{ "tag": "v1", "new_sqlite_classes": ["ChatAgent"] }],
}
```

</WranglerConfig>

핵심 설정:

- `ai`는 Workers AI를 바인딩합니다. API 키가 필요 없습니다
- `durable_objects`는 chat agent class를 등록합니다
- `new_sqlite_classes`는 메시지 영속화를 위한 SQLite storage를 활성화합니다

## 3. 서버 작성

`src/server.ts`를 만듭니다. 에이전트는 여기에서 동작합니다.

<TypeScriptExample>

```ts
import { AIChatAgent } from "@cloudflare/ai-chat";
import { routeAgentRequest } from "agents";
import { createWorkersAI } from "workers-ai-provider";
import {
	streamText,
	convertToModelMessages,
	pruneMessages,
	tool,
	stepCountIs,
} from "ai";
import { z } from "zod";

export class ChatAgent extends AIChatAgent {
	async onChatMessage() {
		const workersai = createWorkersAI({ binding: this.env.AI });

		const result = streamText({
			model: workersai("@cf/meta/llama-4-scout-17b-16e-instruct"),
			system:
				"You are a helpful assistant. You can check the weather, " +
				"get the user's timezone, and run calculations.",
			messages: pruneMessages({
				messages: await convertToModelMessages(this.messages),
				toolCalls: "before-last-2-messages",
			}),
			tools: {
				// Server-side tool: runs automatically on the server
				getWeather: tool({
					description: "Get the current weather for a city",
					inputSchema: z.object({
						city: z.string().describe("City name"),
					}),
					execute: async ({ city }) => {
						// Replace with a real weather API in production
						const conditions = ["sunny", "cloudy", "rainy"];
						const temp = Math.floor(Math.random() * 30) + 5;
						return {
							city,
							temperature: temp,
							condition:
								conditions[Math.floor(Math.random() * conditions.length)],
						};
					},
				}),

				// Client-side tool: no execute function — the browser handles it
				getUserTimezone: tool({
					description: "Get the user's timezone from their browser",
					inputSchema: z.object({}),
				}),

				// Approval tool: requires user confirmation before executing
				calculate: tool({
					description:
						"Perform a math calculation with two numbers. " +
						"Requires user approval for large numbers.",
					inputSchema: z.object({
						a: z.number().describe("First number"),
						b: z.number().describe("Second number"),
						operator: z
							.enum(["+", "-", "*", "/", "%"])
							.describe("Arithmetic operator"),
					}),
					needsApproval: async ({ a, b }) =>
						Math.abs(a) > 1000 || Math.abs(b) > 1000,
					execute: async ({ a, b, operator }) => {
						const ops: Record<string, (x: number, y: number) => number> = {
							"+": (x, y) => x + y,
							"-": (x, y) => x - y,
							"*": (x, y) => x * y,
							"/": (x, y) => x / y,
							"%": (x, y) => x % y,
						};
						if (operator === "/" && b === 0) {
							return { error: "Division by zero" };
						}
						return {
							expression: `${a} ${operator} ${b}`,
							result: ops[operator](a, b),
						};
					},
				}),
			},
			stopWhen: stepCountIs(5),
		});

		return result.toUIMessageStreamResponse();
	}
}

export default {
	async fetch(request: Request, env: Env) {
		return (
			(await routeAgentRequest(request, env)) ||
			new Response("Not found", { status: 404 })
		);
	},
} satisfies ExportedHandler<Env>;
```

</TypeScriptExample>

### 각 tool type의 역할

| Tool              | `execute`? | `needsApproval`?    | Behavior                                        |
| ----------------- | ---------- | ------------------- | ----------------------------------------------- |
| `getWeather`      | Yes        | No                  | 서버에서 자동 실행됨                            |
| `getUserTimezone` | No         | No                  | 클라이언트로 전달되며 브라우저가 결과를 제공함 |
| `calculate`       | Yes        | Yes (large numbers) | 사용자 승인을 위해 멈춘 뒤 서버에서 실행됨     |

## 4. 클라이언트 작성

`src/client.tsx`를 만듭니다.

<TypeScriptExample>

```ts
import { useAgent } from "agents/react";
import { useAgentChat } from "@cloudflare/ai-chat/react";

function Chat() {
	const agent = useAgent({ agent: "ChatAgent" });

	const { messages, sendMessage, clearHistory, addToolApprovalResponse, status } =
		useAgentChat({
			agent,
			// Handle client-side tools (tools with no server execute function)
			onToolCall: async ({ toolCall, addToolOutput }) => {
				if (toolCall.toolName === "getUserTimezone") {
					addToolOutput({
						toolCallId: toolCall.toolCallId,
						output: {
							timezone: Intl.DateTimeFormat().resolvedOptions().timeZone,
							localTime: new Date().toLocaleTimeString(),
						},
					});
				}
			},
		});

	return (
		<div>
			<div>
				{messages.map((msg) => (
					<div key={msg.id}>
						<strong>{msg.role}:</strong>
						{msg.parts.map((part, i) => {
							if (part.type === "text") {
								return <span key={i}>{part.text}</span>;
							}

							// Render approval UI for tools that need confirmation
							if (
								part.type === "tool" &&
								part.state === "approval-required"
							) {
								return (
									<div key={part.toolCallId}>
										<p>
											Approve <strong>{part.toolName}</strong>?
										</p>
										<pre>{JSON.stringify(part.input, null, 2)}</pre>
										<button
											onClick={() =>
												addToolApprovalResponse({
													id: part.toolCallId,
													approved: true,
												})
											}
										>
											Approve
										</button>
										<button
											onClick={() =>
												addToolApprovalResponse({
													id: part.toolCallId,
													approved: false,
												})
											}
										>
											Reject
										</button>
									</div>
								);
							}

							// Show completed tool results
							if (
								part.type === "tool" &&
								part.state === "output-available"
							) {
								return (
									<details key={part.toolCallId}>
										<summary>{part.toolName} result</summary>
										<pre>{JSON.stringify(part.output, null, 2)}</pre>
									</details>
								);
							}

							return null;
						})}
					</div>
				))}
			</div>

			<form
				onSubmit={(e) => {
					e.preventDefault();
					const input = e.currentTarget.elements.namedItem(
						"message",
					) as HTMLInputElement;
					sendMessage({ text: input.value });
					input.value = "";
				}}
			>
				<input name="message" placeholder="Try: What's the weather in Paris?" />
				<button type="submit" disabled={status === "streaming"}>
					Send
				</button>
			</form>

			<button onClick={clearHistory}>Clear history</button>
		</div>
	);
}

export default function App() {
	return <Chat />;
}
```

</TypeScriptExample>

### 핵심 클라이언트 개념

- **`useAgent`**는 WebSocket을 통해 `ChatAgent`에 연결합니다
- **`useAgentChat`**는 채팅 lifecycle(메시지, 스트리밍, 도구)을 관리합니다
- **`onToolCall`**은 클라이언트 측 도구를 처리합니다. LLM이 `getUserTimezone`을 호출하면 브라우저가 결과를 제공하고 대화는 자동으로 이어집니다
- **`addToolApprovalResponse`**는 `needsApproval`이 있는 도구를 승인하거나 거절합니다
- 메시지, 스트리밍, 재개는 모두 자동 처리됩니다

## 5. 로컬에서 실행

types를 생성하고 dev server를 시작합니다.

```sh
npx wrangler types
npm run dev
```

다음 프롬프트를 시도해 보세요.

- **"What is the weather in Tokyo?"** — 서버 측 `getWeather` 도구 호출
- **"What timezone am I in?"** — 클라이언트 측 `getUserTimezone` 도구 호출(브라우저가 답변 제공)
- **"What is 5000 times 3?"** — 실행 전 승인 UI가 나타남(1000 초과 숫자)

## 6. 배포

```sh
npx wrangler deploy
```

이제 agent는 Cloudflare 전역 네트워크에서 실행됩니다. 메시지는 SQLite에 저장되고, 스트림은 연결이 끊겨도 재개되며, 유휴 상태일 때는 하이버네이션되어 리소스를 절약합니다.

## 무엇을 만들었나요?

이 chat agent에는 다음이 포함됩니다.

- **Streaming AI responses** via Workers AI(API 키 없음)
- **Message persistence** in SQLite — 대화가 재시작 이후에도 유지됨
- **Server-side tools**가 자동 실행됨
- **Client-side tools**가 브라우저에서 실행되고 결과를 LLM에 다시 전달함
- **Human-in-the-loop approval** for sensitive operations
- **Resumable streaming** — 클라이언트가 스트림 중간에 끊겨도 이어서 재개

## 다음 단계

<LinkCard
	title="Chat agents API reference"
	href="/agents/api-reference/chat-agents/"
	description="AIChatAgent와 useAgentChat 전체 레퍼런스 — providers, storage, 고급 패턴."
/>

<LinkCard
	title="Store and sync state"
	href="/agents/api-reference/store-and-sync-state/"
	description="채팅 메시지 외의 실시간 상태를 추가하세요."
/>

<LinkCard
	title="Callable methods"
	href="/agents/api-reference/callable-methods/"
	description="agent 메서드를 클라이언트용 typed RPC로 노출하세요."
/>

<LinkCard
	title="Human-in-the-loop"
	href="/agents/concepts/human-in-the-loop/"
	description="승인 플로우와 수동 개입 패턴을 더 깊이 다룹니다."
/>
