---
summary: >-
  Add a custom header to requests sent to the origin server with the current
  timestamp in hexadecimal format.
tags:
  - Headers
  - Request modification
products:
  - snippets
pcx_content_type: example
title: 요청 헤더에 HEX 타임스탬프 추가
description: >-
  디버깅, 추적, 또는 사용자 정의 라우팅 목적으로 hexadecimal 형식의 현재 타임 스탬프를 사용하여 원래 서버로 전송되는 사용자 정의
  헤더를 추가하십시오.
translation_locale: ko-KR
translation_source_html: 'https://developers.cloudflare.com/rules/snippets/examples/hex-timestamp/'
translation_source_md: >-
  https://developers.cloudflare.com/rules/snippets/examples/hex-timestamp/index.md
translation_source_kind: repo-mdx
translation_snapshot: '2026-03-13'
translation_upstream_ref: 66ee2dcd3715f1ca46e70954a3cda63f6cbebc75

---
```js
export default {
	async fetch(request) {
		//현재 타임스탬프 받기
		const timestamp = Date.now();

		//timetamp를 hexadecimal 형식으로 변환
		const hexTimestamp = timestamp.toString(16);

		//요청을 복제하고 사용자 정의 헤더를 추가
		const modifiedRequest = new Request(request, {
			headers: new Headers(request.headers),
		});
		modifiedRequest.headers.set("X-Hex-Timestamp", hexTimestamp);

		//debugging를 위한 custom header를 로그인하십시오
		console.log(`X-Hex-Timestamp: ${hexTimestamp}`);

		//자주 묻는 질문
		const response = await fetch(modifiedRequest);

		return response;
	},
};
```
