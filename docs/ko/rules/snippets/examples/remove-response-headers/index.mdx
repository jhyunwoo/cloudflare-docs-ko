---
summary: Remove from response all headers that start with a certain name.
tags:
  - Response modification
products:
  - snippets
pcx_content_type: example
title: 응답 헤더를 제거
description: 특정 이름으로 시작하는 모든 헤더에서 제거하십시오.
translation_locale: ko-KR
translation_source_html: >-
  https://developers.cloudflare.com/rules/snippets/examples/remove-response-headers/
translation_source_md: >-
  https://developers.cloudflare.com/rules/snippets/examples/remove-response-headers/index.md
translation_source_kind: repo-mdx
translation_snapshot: '2026-03-13'
translation_upstream_ref: 66ee2dcd3715f1ca46e70954a3cda63f6cbebc75

---
```js
export default {
	async fetch(request) {
		//제거하려는 헤더의 접두사 정의
		const headerPrefix = "x-header-";

		//출처에서 응답 받기
		const response = await fetch(request);

		//응답 헤더를 수정하는 새로운 헤더 객체 만들기
		const newHeaders = new Headers(response.headers);

		//지정된 접두사로 시작하는 헤더 제거
		for (const [key] of newHeaders.entries()) {
			if (key.startsWith(headerPrefix)) {
				newHeaders.delete(key);
			}
		}

		//업데이트 된 헤더와 수정 된 응답을 반환
		return new Response(response.body, {
			status: response.status,
			headers: newHeaders,
		});
	},
};
```
