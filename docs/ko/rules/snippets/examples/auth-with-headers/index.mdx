---
summary: >-
  Allow or deny a request based on a known pre-shared key in a header. This is
  not meant to replace the [WebCrypto API](/workers/runtime-apis/web-crypto/).
tags:
  - Authentication
  - Request modification
products:
  - snippets
pcx_content_type: example
title: 머리에 Auth
description: >-
  헤더의 알려진 사전 공유 키에 따라 요청을 허용하거나 거부 할 수 있습니다. 이것은 [WebCrypto API]
  (/workers/runtime-apis/web-crypto/)를 대체하는 것은 의미하지 않습니다.
translation_locale: ko-KR
translation_source_html: 'https://developers.cloudflare.com/rules/snippets/examples/auth-with-headers/'
translation_source_md: >-
  https://developers.cloudflare.com/rules/snippets/examples/auth-with-headers/index.md
translation_source_kind: repo-mdx
translation_snapshot: '2026-03-13'
translation_upstream_ref: 66ee2dcd3715f1ca46e70954a3cda63f6cbebc75

---
:::caution\[제작시 주의사항]
예제 코드에는 일반적인 헤더 키와 값이 포함되어 있습니다.`X-Custom-PSK`·`mypresharedkey`. 귀하의 리소스를 보호하려면 코드를 저장하기 전에 Snippets 편집기의 헤더 키와 값을 변경하십시오.
주요 특징

```js
export default {
	async fetch(request) {
		/** 이름
* @param {문자} PRESHARED AUTH HEADER KEY   테크니컬 열쇠를 검사하는 주문 우두머리
* @param {문자} PRESHARED AUTH HEADER  밸브 하드 코딩 키 값*/
		const PRESHARED_AUTH_HEADER_KEY = "X-Custom-PSK";
		const PRESHARED_AUTH_HEADER_VALUE = "mypresharedkey";
		const psk = request.headers.get(PRESHARED_AUTH_HEADER_KEY);

		if (psk === PRESHARED_AUTH_HEADER_VALUE) {
			//정확한 preshared 헤더 키 공급. 원래의 Fetch 요청.
			return fetch(request);
		}

		//잘못된 키 공급. 요청을 거부합니다.
		return new Response("Sorry, you have supplied an invalid key.", {
			status: 403,
		});
	},
};
```
