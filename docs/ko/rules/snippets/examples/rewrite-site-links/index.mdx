---
summary: Dynamically rewrite links in HTML responses.
tags:
  - Response modification
products:
  - snippets
pcx_content_type: example
title: HTML 페이지에 링크 쓰기
description: HTML 응답에서 동적인 리깅 링크. 사이트 마이그레이션 및 브랜드 업데이트에 유용합니다.
translation_locale: ko-KR
translation_source_html: 'https://developers.cloudflare.com/rules/snippets/examples/rewrite-site-links/'
translation_source_md: >-
  https://developers.cloudflare.com/rules/snippets/examples/rewrite-site-links/index.md
translation_source_kind: repo-mdx
translation_snapshot: '2026-03-13'
translation_upstream_ref: 66ee2dcd3715f1ca46e70954a3cda63f6cbebc75

---
```js
export default {
	async fetch(request) {
		//여기에 오래된 호스트 이름을 정의합니다.
		const OLD_URL = "oldsite.com";
		//그런 다음 새로운 hostname을 추가하십시오.
		const NEW_URL = "newsite.com";

		class AttributeRewriter {
			constructor(attributeName) {
				this.attributeName = attributeName;
			}
			element(element) {
				const attribute = element.getAttribute(this.attributeName);
				if (attribute) {
					element.setAttribute(
						this.attributeName,
						attribute.replace(OLD_URL, NEW_URL),
					);
				}
			}
		}

		const rewriter = new HTMLRewriter()
			.on("a", new AttributeRewriter("href"))
			.on("img", new AttributeRewriter("src"));

		const res = await fetch(request);
		if (!res.headers.has("Content-Type")) {
			return res;
		}
		const contentType = res.headers.get("Content-Type");
		if (typeof contentType !== "string") {
			return res;
		}

		//응답이 HTML이라면 변환 할 수 있습니다.
		//HTMLRewriter -- 그렇지 않으면, 통과해야 합니다
		if (contentType.startsWith("text/html")) {
			return rewriter.transform(res);
		} else {
			return res;
		}
	},
};
```
