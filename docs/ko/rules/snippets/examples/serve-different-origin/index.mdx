---
summary: >-
  If response to the original request is not `200 OK` or a redirect, send to
  another origin.
tags:
  - Redirects
products:
  - snippets
pcx_content_type: example
title: 근원 응답에 근거를 둔 다른 근원에 노선
description: 원래 요청에 응답하면 `200 OK` 또는 리디렉션이 아닌 다른 기원으로 보내집니다.
translation_locale: ko-KR
translation_source_html: >-
  https://developers.cloudflare.com/rules/snippets/examples/serve-different-origin/
translation_source_md: >-
  https://developers.cloudflare.com/rules/snippets/examples/serve-different-origin/index.md
translation_source_kind: repo-mdx
translation_snapshot: '2026-03-13'
translation_upstream_ref: 66ee2dcd3715f1ca46e70954a3cda63f6cbebc75

---
```js
export default {
	async fetch(request) {
		//원래 요청을 원산지로 보내십시오.
		const response = await fetch(request);

		//응답이 200 OK 또는 리디렉션되지 않은 경우, 다른 기원으로 보내
		if (!response.ok && !response.redirected) {
			//첫째로, clone 새로운 요구를 건설하는 본래 요구
			const newRequest = new Request(request);
			//새로운 기원의 재선명 요청을 식별하기 위해 헤더를 추가하십시오.
			newRequest.headers.set("X-Rerouted", "1");
			//원래 URL을 복제
			const url = new URL(request.url);
			//다른 기원 / hostname에 요청 보내기
			url.hostname = "example.com";
			//원산지의 새로운 요청에 대한 응답
			return await fetch(url, newRequest);
		}

		//응답이 200 OK 또는 리디렉션 인 경우, 제공
		return response;
	},
};
```
