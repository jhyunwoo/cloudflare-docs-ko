---
summary: Remove certain query strings from a request before passing to the origin.
tags:
  - Request modification
products:
  - snippets
pcx_content_type: example
title: 요청을 전달하기 전에 쿼리 문자열을 제거
description: 전달하기 전에 요청에서 특정 쿼리 문자열을 제거하십시오.
translation_locale: ko-KR
translation_source_html: >-
  https://developers.cloudflare.com/rules/snippets/examples/remove-query-strings/
translation_source_md: >-
  https://developers.cloudflare.com/rules/snippets/examples/remove-query-strings/index.md
translation_source_kind: repo-mdx
translation_snapshot: '2026-03-13'
translation_upstream_ref: 66ee2dcd3715f1ca46e70954a3cda63f6cbebc75

---
```js
export default {
	async fetch(request) {
		//제거 할 쿼리 문자열 정의
		const queryStringsToRemove = ["utm_source", "utm_medium", "utm_campaign"];

		//요청에서 URL을 가져옵니다
		const url = new URL(request.url);

		//지정된 쿼리 문자열 제거
		queryStringsToRemove.forEach((query) => {
			url.searchParams.delete(query);
		});

		//수정된 URL로 새 요청 만들기
		const modifiedRequest = new Request(url, request);

		//자주 묻는 질문
		const response = await fetch(modifiedRequest);

		return response;
	},
};
```
