---
summary: Redirect all requests from one domain to another domain.
tags:
  - Redirects
products:
  - snippets
pcx_content_type: example
title: 한 도메인에서 다른 도메인으로 이동
description: 한 도메인에서 다른 도메인으로 모든 요청을 다시 시작합니다.
translation_locale: ko-KR
translation_source_html: >-
  https://developers.cloudflare.com/rules/snippets/examples/redirect-replaced-domain/
translation_source_md: >-
  https://developers.cloudflare.com/rules/snippets/examples/redirect-replaced-domain/index.md
translation_source_kind: repo-mdx
translation_snapshot: '2026-03-13'
translation_upstream_ref: 66ee2dcd3715f1ca46e70954a3cda63f6cbebc75

---
```js
export default {
	async fetch(request) {
		//응답 리디렉션에서 사용할 변수 정의.
		const base = "https://example.com";
		const statusCode = 301;

		//원래 URL을 복제합니다.
		const url = new URL(request.url);

		//"pathname"과 "search" 변수를 정의하고 복제 된 URL에서 값을 추출합니다.
		const { pathname, search } = url;

		//이전에 선언된 변수를 사용하여 대상 URL을 정의합니다.
		const destinationURL = `${base}${pathname}${search}`;
		console.log(destinationURL);

		//리디렉션으로 응답합니다.
		return Response.redirect(destinationURL, statusCode);
	},
};
```
