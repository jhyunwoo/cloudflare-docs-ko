---
summary: >-
  Use the [bot score
  field](/workers/runtime-apis/request/#incomingrequestcfproperties) to send
  bots to a honeypot.
tags:
  - Redirects
products:
  - snippets
pcx_content_type: example
title: 봇을 허니팟으로 전송
description: >-
  [봇 점수 필드](/workers/runtime-apis/request/#incomingrequestcfproperties)를 사용하여 봇을
  허니팟으로 보낼 수 있습니다.
translation_locale: ko-KR
translation_source_html: 'https://developers.cloudflare.com/rules/snippets/examples/bots-to-honeypot/'
translation_source_md: >-
  https://developers.cloudflare.com/rules/snippets/examples/bots-to-honeypot/index.md
translation_source_kind: repo-mdx
translation_snapshot: '2026-03-13'
translation_upstream_ref: 66ee2dcd3715f1ca46e70954a3cda63f6cbebc75

---
```js
export default {
	async fetch(request) {
		const response = await fetch(request);

		//더 이상 immutable 인 응답을 복제
		const newResponse = new Response(response.body, response);

		if (request.cf.botManagement.score < 30) {
			const honeypot = "https://example.com/";
			return await fetch(honeypot, request);
		} else {
			return newResponse;
		}
	},
};
```
