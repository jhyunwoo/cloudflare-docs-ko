---
summary: Redirect a response based on the country code in the header of a visitor.
tags:
  - Localization
  - Redirects
products:
  - snippets
preview:
  - true
pcx_content_type: example
title: 국가 코드 리디렉션
description: 방문자의 헤더에 국가 코드를 기반으로 응답을 다시.
translation_locale: ko-KR
translation_source_html: >-
  https://developers.cloudflare.com/rules/snippets/examples/country-code-redirect/
translation_source_md: >-
  https://developers.cloudflare.com/rules/snippets/examples/country-code-redirect/index.md
translation_source_kind: repo-mdx
translation_snapshot: '2026-03-13'
translation_upstream_ref: 66ee2dcd3715f1ca46e70954a3cda63f6cbebc75

---
```js
export default {
	async fetch(request) {
		/** 이름
* URL의 맵으로 리디렉션
* @param {Object} 국가지도*/
		const countryMap = {
			//국가 코드를 대체하고 대상 URL을 하나의 경우에 적용.
			US: "https://example.com/us",
			EU: "https://example.com/eu",
		};

		//cf 개체를 사용하여 요청의 국가를 얻기
		//cf 객체에 더: https://developers.cloudflare.com/workers/runtime-apis/request#incomingrequestcfproperties
		const country = request.cf.country;

		//국가가 null이 아닌 경우, 위의 국가지도에서 정의된다.
		if (country != null && country in countryMap) {
			const url = countryMap[country];
			//최종 출력에서 이 로깅 문 제거.
			console.log(
				`Based on ${country}-based request, your user would go to ${url}.`,
			);
			return Response.redirect(url);

			//국가가지도에없는 경우 다른 페이지를 반환합니다.
		} else {
			return fetch("https://example.com", request);
		}
	},
};
```
